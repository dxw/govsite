/*
 * Foundation Responsive Library
 * http://foundation.zurb.com
 * Copyright 2014, ZURB
 * Free to use under the MIT license.
 * http://www.opensource.org/licenses/mit-license.php
*/
(function(a, b, c, d) {
    "use strict";
    var e = function(b) {
        var c = b.length;
        var d = a("head");
        while (c--) {
            if (d.has("." + b[c]).length === 0) {
                d.append('<meta class="' + b[c] + '" />');
            }
        }
    };
    e([ "foundation-mq-small", "foundation-mq-medium", "foundation-mq-large", "foundation-mq-xlarge", "foundation-mq-xxlarge", "foundation-data-attribute-namespace" ]);
    // Enable FastClick if present
    a(function() {
        if (typeof FastClick !== "undefined") {
            // Don't attach to body if undefined
            if (typeof c.body !== "undefined") {
                FastClick.attach(c.body);
            }
        }
    });
    // private Fast Selector wrapper,
    // returns jQuery object. Only use where
    // getElementById is not available.
    var f = function(b, d) {
        if (typeof b === "string") {
            if (d) {
                var e;
                if (d.jquery) {
                    e = d[0];
                    if (!e) return d;
                } else {
                    e = d;
                }
                return a(e.querySelectorAll(b));
            }
            return a(c.querySelectorAll(b));
        }
        return a(b, d);
    };
    // Namespace functions.
    var g = function(a) {
        var b = [];
        if (!a) b.push("data");
        if (this.namespace.length > 0) b.push(this.namespace);
        b.push(this.name);
        return b.join("-");
    };
    var h = function(a) {
        var b = a.split("-"), c = b.length, d = [];
        while (c--) {
            if (c !== 0) {
                d.push(b[c]);
            } else {
                if (this.namespace.length > 0) {
                    d.push(this.namespace, b[c]);
                } else {
                    d.push(b[c]);
                }
            }
        }
        return d.reverse().join("-");
    };
    // Event binding and data-options updating.
    var i = function(b, c) {
        var d = this, e = !f(this).data(this.attr_name(true));
        if (f(this.scope).is("[" + this.attr_name() + "]")) {
            f(this.scope).data(this.attr_name(true) + "-init", a.extend({}, this.settings, c || b, this.data_options(f(this.scope))));
            if (e) {
                this.events(this.scope);
            }
        } else {
            f("[" + this.attr_name() + "]", this.scope).each(function() {
                var e = !f(this).data(d.attr_name(true) + "-init");
                f(this).data(d.attr_name(true) + "-init", a.extend({}, d.settings, c || b, d.data_options(f(this))));
                if (e) {
                    d.events(this);
                }
            });
        }
        // # Patch to fix #5043 to move this *after* the if/else clause in order for Backbone and similar frameworks to have improved control over event binding and data-options updating.
        if (typeof b === "string") {
            return this[b].call(this, c);
        }
    };
    var j = function(a, b) {
        function c() {
            b(a[0]);
        }
        function d() {
            this.one("load", c);
            if (/MSIE (\d+\.\d+);/.test(navigator.userAgent)) {
                var a = this.attr("src"), b = a.match(/\?/) ? "&" : "?";
                b += "random=" + new Date().getTime();
                this.attr("src", a + b);
            }
        }
        if (!a.attr("src")) {
            c();
            return;
        }
        if (a[0].complete || a[0].readyState === 4) {
            c();
        } else {
            d.call(a);
        }
    };
    /*
    https://github.com/paulirish/matchMedia.js
  */
    b.matchMedia = b.matchMedia || function(a) {
        "use strict";
        var b, c = a.documentElement, d = c.firstElementChild || c.firstChild, // fakeBody required for <FF4 when executed in <head>
        e = a.createElement("body"), f = a.createElement("div");
        f.id = "mq-test-1";
        f.style.cssText = "position:absolute;top:-100em";
        e.style.background = "none";
        e.appendChild(f);
        return function(a) {
            f.innerHTML = '&shy;<style media="' + a + '"> #mq-test-1 { width: 42px; }</style>';
            c.insertBefore(e, d);
            b = f.offsetWidth === 42;
            c.removeChild(e);
            return {
                matches: b,
                media: a
            };
        };
    }(c);
    /*
   * jquery.requestAnimationFrame
   * https://github.com/gnarf37/jquery-requestAnimationFrame
   * Requires jQuery 1.8+
   *
   * Copyright (c) 2012 Corey Frang
   * Licensed under the MIT license.
   */
    (function(a) {
        // requestAnimationFrame polyfill adapted from Erik Möller
        // fixes from Paul Irish and Tino Zijdel
        // http://paulirish.com/2011/requestanimationframe-for-smart-animating/
        // http://my.opera.com/emoller/blog/2011/12/20/requestanimationframe-for-smart-er-animating
        var c, d = 0, e = [ "webkit", "moz" ], f = b.requestAnimationFrame, g = b.cancelAnimationFrame, h = "undefined" !== typeof jQuery.fx;
        for (;d < e.length && !f; d++) {
            f = b[e[d] + "RequestAnimationFrame"];
            g = g || b[e[d] + "CancelAnimationFrame"] || b[e[d] + "CancelRequestAnimationFrame"];
        }
        function i() {
            if (c) {
                f(i);
                if (h) {
                    jQuery.fx.tick();
                }
            }
        }
        if (f) {
            // use rAF
            b.requestAnimationFrame = f;
            b.cancelAnimationFrame = g;
            if (h) {
                jQuery.fx.timer = function(a) {
                    if (a() && jQuery.timers.push(a) && !c) {
                        c = true;
                        i();
                    }
                };
                jQuery.fx.stop = function() {
                    c = false;
                };
            }
        } else {
            // polyfill
            b.requestAnimationFrame = function(a) {
                var c = new Date().getTime(), e = Math.max(0, 16 - (c - d)), f = b.setTimeout(function() {
                    a(c + e);
                }, e);
                d = c + e;
                return f;
            };
            b.cancelAnimationFrame = function(a) {
                clearTimeout(a);
            };
        }
    })(jQuery);
    function k(a) {
        if (typeof a === "string" || a instanceof String) {
            a = a.replace(/^['\\/"]+|(;\s?})+|['\\/"]+$/g, "");
        }
        return a;
    }
    b.Foundation = {
        name: "Foundation",
        version: "5.4.5",
        media_queries: {
            small: f(".foundation-mq-small").css("font-family").replace(/^[\/\\'"]+|(;\s?})+|[\/\\'"]+$/g, ""),
            medium: f(".foundation-mq-medium").css("font-family").replace(/^[\/\\'"]+|(;\s?})+|[\/\\'"]+$/g, ""),
            large: f(".foundation-mq-large").css("font-family").replace(/^[\/\\'"]+|(;\s?})+|[\/\\'"]+$/g, ""),
            xlarge: f(".foundation-mq-xlarge").css("font-family").replace(/^[\/\\'"]+|(;\s?})+|[\/\\'"]+$/g, ""),
            xxlarge: f(".foundation-mq-xxlarge").css("font-family").replace(/^[\/\\'"]+|(;\s?})+|[\/\\'"]+$/g, "")
        },
        stylesheet: a("<style></style>").appendTo("head")[0].sheet,
        global: {
            namespace: d
        },
        init: function(a, c, d, e, g) {
            var h = [ a, d, e, g ], i = [];
            // check RTL
            this.rtl = /rtl/i.test(f("html").attr("dir"));
            // set foundation global scope
            this.scope = a || this.scope;
            this.set_namespace();
            if (c && typeof c === "string" && !/reflow/i.test(c)) {
                if (this.libs.hasOwnProperty(c)) {
                    i.push(this.init_lib(c, h));
                }
            } else {
                for (var j in this.libs) {
                    i.push(this.init_lib(j, c));
                }
            }
            f(b).load(function() {
                f(b).trigger("resize.fndtn.clearing").trigger("resize.fndtn.dropdown").trigger("resize.fndtn.equalizer").trigger("resize.fndtn.interchange").trigger("resize.fndtn.joyride").trigger("resize.fndtn.magellan").trigger("resize.fndtn.topbar").trigger("resize.fndtn.slider");
            });
            return a;
        },
        init_lib: function(b, c) {
            if (this.libs.hasOwnProperty(b)) {
                this.patch(this.libs[b]);
                if (c && c.hasOwnProperty(b)) {
                    if (typeof this.libs[b].settings !== "undefined") {
                        a.extend(true, this.libs[b].settings, c[b]);
                    } else if (typeof this.libs[b].defaults !== "undefined") {
                        a.extend(true, this.libs[b].defaults, c[b]);
                    }
                    return this.libs[b].init.apply(this.libs[b], [ this.scope, c[b] ]);
                }
                c = c instanceof Array ? c : new Array(c);
                // PATCH: added this line
                return this.libs[b].init.apply(this.libs[b], c);
            }
            return function() {};
        },
        patch: function(a) {
            a.scope = this.scope;
            a.namespace = this.global.namespace;
            a.rtl = this.rtl;
            a["data_options"] = this.utils.data_options;
            a["attr_name"] = g;
            a["add_namespace"] = h;
            a["bindings"] = i;
            a["S"] = this.utils.S;
        },
        inherit: function(a, b) {
            var c = b.split(" "), d = c.length;
            while (d--) {
                if (this.utils.hasOwnProperty(c[d])) {
                    a[c[d]] = this.utils[c[d]];
                }
            }
        },
        set_namespace: function() {
            // Description:
            //    Don't bother reading the namespace out of the meta tag
            //    if the namespace has been set globally in javascript
            //
            // Example:
            //    Foundation.global.namespace = 'my-namespace';
            // or make it an empty string:
            //    Foundation.global.namespace = '';
            //
            //
            // If the namespace has not been set (is undefined), try to read it out of the meta element.
            // Otherwise use the globally defined namespace, even if it's empty ('')
            var b = this.global.namespace === d ? a(".foundation-data-attribute-namespace").css("font-family") : this.global.namespace;
            // Finally, if the namsepace is either undefined or false, set it to an empty string.
            // Otherwise use the namespace value.
            this.global.namespace = b === d || /false/i.test(b) ? "" : b;
        },
        libs: {},
        // methods that can be inherited in libraries
        utils: {
            // Description:
            //    Fast Selector wrapper returns jQuery object. Only use where getElementById
            //    is not available.
            //
            // Arguments:
            //    Selector (String): CSS selector describing the element(s) to be
            //    returned as a jQuery object.
            //
            //    Scope (String): CSS selector describing the area to be searched. Default
            //    is document.
            //
            // Returns:
            //    Element (jQuery Object): jQuery object containing elements matching the
            //    selector within the scope.
            S: f,
            // Description:
            //    Executes a function a max of once every n milliseconds
            //
            // Arguments:
            //    Func (Function): Function to be throttled.
            //
            //    Delay (Integer): Function execution threshold in milliseconds.
            //
            // Returns:
            //    Lazy_function (Function): Function with throttling applied.
            throttle: function(a, b) {
                var c = null;
                return function() {
                    var d = this, e = arguments;
                    if (c == null) {
                        c = setTimeout(function() {
                            a.apply(d, e);
                            c = null;
                        }, b);
                    }
                };
            },
            // Description:
            //    Executes a function when it stops being invoked for n seconds
            //    Modified version of _.debounce() http://underscorejs.org
            //
            // Arguments:
            //    Func (Function): Function to be debounced.
            //
            //    Delay (Integer): Function execution threshold in milliseconds.
            //
            //    Immediate (Bool): Whether the function should be called at the beginning
            //    of the delay instead of the end. Default is false.
            //
            // Returns:
            //    Lazy_function (Function): Function with debouncing applied.
            debounce: function(a, b, c) {
                var d, e;
                return function() {
                    var f = this, g = arguments;
                    var h = function() {
                        d = null;
                        if (!c) e = a.apply(f, g);
                    };
                    var i = c && !d;
                    clearTimeout(d);
                    d = setTimeout(h, b);
                    if (i) e = a.apply(f, g);
                    return e;
                };
            },
            // Description:
            //    Parses data-options attribute
            //
            // Arguments:
            //    El (jQuery Object): Element to be parsed.
            //
            // Returns:
            //    Options (Javascript Object): Contents of the element's data-options
            //    attribute.
            data_options: function(b, c) {
                c = c || "options";
                var d = {}, e, f, g, h = function(a) {
                    var b = Foundation.global.namespace;
                    if (b.length > 0) {
                        return a.data(b + "-" + c);
                    }
                    return a.data(c);
                };
                var i = h(b);
                if (typeof i === "object") {
                    return i;
                }
                g = (i || ":").split(";");
                e = g.length;
                function j(a) {
                    return !isNaN(a - 0) && a !== null && a !== "" && a !== false && a !== true;
                }
                function k(b) {
                    if (typeof b === "string") return a.trim(b);
                    return b;
                }
                while (e--) {
                    f = g[e].split(":");
                    f = [ f[0], f.slice(1).join(":") ];
                    if (/true/i.test(f[1])) f[1] = true;
                    if (/false/i.test(f[1])) f[1] = false;
                    if (j(f[1])) {
                        if (f[1].indexOf(".") === -1) {
                            f[1] = parseInt(f[1], 10);
                        } else {
                            f[1] = parseFloat(f[1]);
                        }
                    }
                    if (f.length === 2 && f[0].length > 0) {
                        d[k(f[0])] = k(f[1]);
                    }
                }
                return d;
            },
            // Description:
            //    Adds JS-recognizable media queries
            //
            // Arguments:
            //    Media (String): Key string for the media query to be stored as in
            //    Foundation.media_queries
            //
            //    Class (String): Class name for the generated <meta> tag
            register_media: function(b, c) {
                if (Foundation.media_queries[b] === d) {
                    a("head").append('<meta class="' + c + '"/>');
                    Foundation.media_queries[b] = k(a("." + c).css("font-family"));
                }
            },
            // Description:
            //    Add custom CSS within a JS-defined media query
            //
            // Arguments:
            //    Rule (String): CSS rule to be appended to the document.
            //
            //    Media (String): Optional media query string for the CSS rule to be
            //    nested under.
            add_custom_rule: function(a, b) {
                if (b === d && Foundation.stylesheet) {
                    Foundation.stylesheet.insertRule(a, Foundation.stylesheet.cssRules.length);
                } else {
                    var c = Foundation.media_queries[b];
                    if (c !== d) {
                        Foundation.stylesheet.insertRule("@media " + Foundation.media_queries[b] + "{ " + a + " }");
                    }
                }
            },
            // Description:
            //    Performs a callback function when an image is fully loaded
            //
            // Arguments:
            //    Image (jQuery Object): Image(s) to check if loaded.
            //
            //    Callback (Function): Function to execute when image is fully loaded.
            image_loaded: function(a, b) {
                var c = this, d = a.length;
                if (d === 0) {
                    b(a);
                }
                a.each(function() {
                    j(c.S(this), function() {
                        d -= 1;
                        if (d === 0) {
                            b(a);
                        }
                    });
                });
            },
            // Description:
            //    Returns a random, alphanumeric string
            //
            // Arguments:
            //    Length (Integer): Length of string to be generated. Defaults to random
            //    integer.
            //
            // Returns:
            //    Rand (String): Pseudo-random, alphanumeric string.
            random_str: function() {
                if (!this.fidx) this.fidx = 0;
                this.prefix = this.prefix || [ this.name || "F", (+new Date()).toString(36) ].join("-");
                return this.prefix + (this.fidx++).toString(36);
            }
        }
    };
    a.fn.foundation = function() {
        var a = Array.prototype.slice.call(arguments, 0);
        return this.each(function() {
            Foundation.init.apply(Foundation, [ this ].concat(a));
            return this;
        });
    };
})(jQuery, window, window.document);

(function(a, b, c, d) {
    "use strict";
    Foundation.libs.abide = {
        name: "abide",
        version: "5.4.5",
        settings: {
            live_validate: true,
            focus_on_invalid: true,
            error_labels: true,
            // labels with a for="inputId" will recieve an `error` class
            timeout: 1e3,
            patterns: {
                alpha: /^[a-zA-Z]+$/,
                alpha_numeric: /^[a-zA-Z0-9]+$/,
                integer: /^[-+]?\d+$/,
                number: /^[-+]?\d*(?:[\.\,]\d+)?$/,
                // amex, visa, diners
                card: /^(?:4[0-9]{12}(?:[0-9]{3})?|5[1-5][0-9]{14}|6(?:011|5[0-9][0-9])[0-9]{12}|3[47][0-9]{13}|3(?:0[0-5]|[68][0-9])[0-9]{11}|(?:2131|1800|35\d{3})\d{11})$/,
                cvv: /^([0-9]){3,4}$/,
                // http://www.whatwg.org/specs/web-apps/current-work/multipage/states-of-the-type-attribute.html#valid-e-mail-address
                email: /^[a-zA-Z0-9.!#$%&'*+\/=?^_`{|}~-]+@[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?(?:\.[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?)+$/,
                url: /^(https?|ftp|file|ssh):\/\/(((([a-zA-Z]|\d|-|\.|_|~|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])|(%[\da-f]{2})|[!\$&'\(\)\*\+,;=]|:)*@)?(((\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-5])\.(\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-5])\.(\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-5])\.(\d|[1-9]\d|1\d\d|2[0-4]\d|25[0-5]))|((([a-zA-Z]|\d|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])|(([a-zA-Z]|\d|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])([a-zA-Z]|\d|-|\.|_|~|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])*([a-zA-Z]|\d|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])))\.)+(([a-zA-Z]|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])|(([a-zA-Z]|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])([a-zA-Z]|\d|-|\.|_|~|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])*([a-zA-Z]|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])))\.?)(:\d*)?)(\/((([a-zA-Z]|\d|-|\.|_|~|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])|(%[\da-f]{2})|[!\$&'\(\)\*\+,;=]|:|@)+(\/(([a-zA-Z]|\d|-|\.|_|~|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])|(%[\da-f]{2})|[!\$&'\(\)\*\+,;=]|:|@)*)*)?)?(\?((([a-zA-Z]|\d|-|\.|_|~|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])|(%[\da-f]{2})|[!\$&'\(\)\*\+,;=]|:|@)|[\uE000-\uF8FF]|\/|\?)*)?(\#((([a-zA-Z]|\d|-|\.|_|~|[\u00A0-\uD7FF\uF900-\uFDCF\uFDF0-\uFFEF])|(%[\da-f]{2})|[!\$&'\(\)\*\+,;=]|:|@)|\/|\?)*)?$/,
                // abc.de
                domain: /^([a-zA-Z0-9]([a-zA-Z0-9\-]{0,61}[a-zA-Z0-9])?\.)+[a-zA-Z]{2,6}$/,
                datetime: /^([0-2][0-9]{3})\-([0-1][0-9])\-([0-3][0-9])T([0-5][0-9])\:([0-5][0-9])\:([0-5][0-9])(Z|([\-\+]([0-1][0-9])\:00))$/,
                // YYYY-MM-DD
                date: /(?:19|20)[0-9]{2}-(?:(?:0[1-9]|1[0-2])-(?:0[1-9]|1[0-9]|2[0-9])|(?:(?!02)(?:0[1-9]|1[0-2])-(?:30))|(?:(?:0[13578]|1[02])-31))$/,
                // HH:MM:SS
                time: /^(0[0-9]|1[0-9]|2[0-3])(:[0-5][0-9]){2}$/,
                dateISO: /^\d{4}[\/\-]\d{1,2}[\/\-]\d{1,2}$/,
                // MM/DD/YYYY
                month_day_year: /^(0[1-9]|1[012])[- \/.](0[1-9]|[12][0-9]|3[01])[- \/.]\d{4}$/,
                // DD/MM/YYYY
                day_month_year: /^(0[1-9]|[12][0-9]|3[01])[- \/.](0[1-9]|1[012])[- \/.]\d{4}$/,
                // #FFF or #FFFFFF
                color: /^#?([a-fA-F0-9]{6}|[a-fA-F0-9]{3})$/
            },
            validators: {
                equalTo: function(a, b, d) {
                    var e = c.getElementById(a.getAttribute(this.add_namespace("data-equalto"))).value, f = a.value, g = e === f;
                    return g;
                }
            }
        },
        timer: null,
        init: function(a, b, c) {
            this.bindings(b, c);
        },
        events: function(b) {
            var c = this, d = c.S(b).attr("novalidate", "novalidate"), e = d.data(this.attr_name(true) + "-init") || {};
            this.invalid_attr = this.add_namespace("data-invalid");
            d.off(".abide").on("submit.fndtn.abide validate.fndtn.abide", function(a) {
                var b = /ajax/i.test(c.S(this).attr(c.attr_name()));
                return c.validate(c.S(this).find("input, textarea, select").get(), a, b);
            }).on("reset", function() {
                return c.reset(a(this));
            }).find("input, textarea, select").off(".abide").on("blur.fndtn.abide change.fndtn.abide", function(a) {
                c.validate([ this ], a);
            }).on("keydown.fndtn.abide", function(a) {
                if (e.live_validate === true) {
                    clearTimeout(c.timer);
                    c.timer = setTimeout(function() {
                        c.validate([ this ], a);
                    }.bind(this), e.timeout);
                }
            });
        },
        reset: function(b) {
            b.removeAttr(this.invalid_attr);
            a(this.invalid_attr, b).removeAttr(this.invalid_attr);
            a(".error", b).not("small").removeClass("error");
        },
        validate: function(a, b, c) {
            var d = this.parse_patterns(a), e = d.length, f = this.S(a[0]).closest("form"), g = /submit/.test(b.type);
            // Has to count up to make sure the focus gets applied to the top error
            for (var h = 0; h < e; h++) {
                if (!d[h] && (g || c)) {
                    if (this.settings.focus_on_invalid) a[h].focus();
                    f.trigger("invalid");
                    this.S(a[h]).closest("form").attr(this.invalid_attr, "");
                    return false;
                }
            }
            if (g || c) {
                f.trigger("valid");
            }
            f.removeAttr(this.invalid_attr);
            if (c) return false;
            return true;
        },
        parse_patterns: function(a) {
            var b = a.length, c = [];
            while (b--) {
                c.push(this.pattern(a[b]));
            }
            return this.check_validation_and_apply_styles(c);
        },
        pattern: function(a) {
            var b = a.getAttribute("type"), c = typeof a.getAttribute("required") === "string";
            var d = a.getAttribute("pattern") || "";
            if (this.settings.patterns.hasOwnProperty(d) && d.length > 0) {
                return [ a, this.settings.patterns[d], c ];
            } else if (d.length > 0) {
                return [ a, new RegExp(d), c ];
            }
            if (this.settings.patterns.hasOwnProperty(b)) {
                return [ a, this.settings.patterns[b], c ];
            }
            d = /.*/;
            return [ a, d, c ];
        },
        check_validation_and_apply_styles: function(b) {
            var c = b.length, d = [], e = this.S(b[0][0]).closest("[data-" + this.attr_name(true) + "]"), f = e.data(this.attr_name(true) + "-init") || {};
            while (c--) {
                var g = b[c][0], h = b[c][2], i = g.value.trim(), j = this.S(g).parent(), k = g.getAttribute(this.add_namespace("data-abide-validator")), l = g.type === "radio", m = g.type === "checkbox", n = this.S('label[for="' + g.getAttribute("id") + '"]'), o = h ? g.value.length > 0 : true, p = [];
                var q, r;
                // support old way to do equalTo validations
                if (g.getAttribute(this.add_namespace("data-equalto"))) {
                    k = "equalTo";
                }
                if (!j.is("label")) {
                    q = j;
                } else {
                    q = j.parent();
                }
                if (k) {
                    r = this.settings.validators[k].apply(this, [ g, h, q ]);
                    p.push(r);
                }
                if (l && h) {
                    p.push(this.valid_radio(g, h));
                } else if (m && h) {
                    p.push(this.valid_checkbox(g, h));
                } else {
                    if (b[c][1].test(i) && o || !h && g.value.length < 1 || a(g).attr("disabled")) {
                        p.push(true);
                    } else {
                        p.push(false);
                    }
                    p = [ p.every(function(a) {
                        return a;
                    }) ];
                    if (p[0]) {
                        this.S(g).removeAttr(this.invalid_attr);
                        g.setAttribute("aria-invalid", "false");
                        g.removeAttribute("aria-describedby");
                        q.removeClass("error");
                        if (n.length > 0 && this.settings.error_labels) {
                            n.removeClass("error").removeAttr("role");
                        }
                        a(g).triggerHandler("valid");
                    } else {
                        this.S(g).attr(this.invalid_attr, "");
                        g.setAttribute("aria-invalid", "true");
                        // Try to find the error associated with the input
                        var s = q.find("small.error, span.error");
                        var t = s.length > 0 ? s[0].id : "";
                        if (t.length > 0) g.setAttribute("aria-describedby", t);
                        // el.setAttribute('aria-describedby', $(el).find('.error')[0].id);
                        q.addClass("error");
                        if (n.length > 0 && this.settings.error_labels) {
                            n.addClass("error").attr("role", "alert");
                        }
                        a(g).triggerHandler("invalid");
                    }
                    d.push(p[0]);
                }
            }
            d = [ d.every(function(a) {
                return a;
            }) ];
            return d;
        },
        valid_checkbox: function(a, b) {
            var a = this.S(a), c = a.is(":checked") || !b;
            if (c) {
                a.removeAttr(this.invalid_attr).parent().removeClass("error");
            } else {
                a.attr(this.invalid_attr, "").parent().addClass("error");
            }
            return c;
        },
        valid_radio: function(a, b) {
            var c = a.getAttribute("name"), d = this.S(a).closest("[data-" + this.attr_name(true) + "]").find("[name='" + c + "']"), e = d.length, f = false;
            // Has to count up to make sure the focus gets applied to the top error
            for (var g = 0; g < e; g++) {
                if (d[g].checked) f = true;
            }
            // Has to count up to make sure the focus gets applied to the top error
            for (var g = 0; g < e; g++) {
                if (f) {
                    this.S(d[g]).removeAttr(this.invalid_attr).parent().removeClass("error");
                } else {
                    this.S(d[g]).attr(this.invalid_attr, "").parent().addClass("error");
                }
            }
            return f;
        },
        valid_equal: function(a, b, d) {
            var e = c.getElementById(a.getAttribute(this.add_namespace("data-equalto"))).value, f = a.value, g = e === f;
            if (g) {
                this.S(a).removeAttr(this.invalid_attr);
                d.removeClass("error");
                if (label.length > 0 && settings.error_labels) label.removeClass("error");
            } else {
                this.S(a).attr(this.invalid_attr, "");
                d.addClass("error");
                if (label.length > 0 && settings.error_labels) label.addClass("error");
            }
            return g;
        },
        valid_oneof: function(a, b, c, d) {
            var a = this.S(a), e = this.S("[" + this.add_namespace("data-oneof") + "]"), f = e.filter(":checked").length > 0;
            if (f) {
                a.removeAttr(this.invalid_attr).parent().removeClass("error");
            } else {
                a.attr(this.invalid_attr, "").parent().addClass("error");
            }
            if (!d) {
                var g = this;
                e.each(function() {
                    g.valid_oneof.call(g, this, null, null, true);
                });
            }
            return f;
        }
    };
})(jQuery, window, window.document);

(function(a, b, c, d) {
    "use strict";
    Foundation.libs.accordion = {
        name: "accordion",
        version: "5.4.5",
        settings: {
            active_class: "active",
            multi_expand: false,
            toggleable: true,
            callback: function() {}
        },
        init: function(a, b, c) {
            this.bindings(b, c);
        },
        events: function() {
            var b = this;
            var c = this.S;
            c(this.scope).off(".fndtn.accordion").on("click.fndtn.accordion", "[" + this.attr_name() + "] > dd > a", function(d) {
                var e = c(this).closest("[" + b.attr_name() + "]"), f = b.attr_name() + "=" + e.attr(b.attr_name()), g = e.data(b.attr_name(true) + "-init"), h = c("#" + this.href.split("#")[1]), i = a("> dd", e), j = i.children(".content"), k = j.filter("." + g.active_class);
                d.preventDefault();
                if (e.attr(b.attr_name())) {
                    j = j.add("[" + f + "] dd > .content");
                    i = i.add("[" + f + "] dd");
                }
                if (g.toggleable && h.is(k)) {
                    h.parent("dd").toggleClass(g.active_class, false);
                    h.toggleClass(g.active_class, false);
                    g.callback(h);
                    h.triggerHandler("toggled", [ e ]);
                    e.triggerHandler("toggled", [ h ]);
                    return;
                }
                if (!g.multi_expand) {
                    j.removeClass(g.active_class);
                    i.removeClass(g.active_class);
                }
                h.addClass(g.active_class).parent().addClass(g.active_class);
                g.callback(h);
                h.triggerHandler("toggled", [ e ]);
                e.triggerHandler("toggled", [ h ]);
            });
        },
        off: function() {},
        reflow: function() {}
    };
})(jQuery, window, window.document);

(function(a, b, c, d) {
    "use strict";
    Foundation.libs.alert = {
        name: "alert",
        version: "5.4.5",
        settings: {
            callback: function() {}
        },
        init: function(a, b, c) {
            this.bindings(b, c);
        },
        events: function() {
            var b = this, c = this.S;
            a(this.scope).off(".alert").on("click.fndtn.alert", "[" + this.attr_name() + "] .close", function(a) {
                var d = c(this).closest("[" + b.attr_name() + "]"), e = d.data(b.attr_name(true) + "-init") || b.settings;
                a.preventDefault();
                if (Modernizr.csstransitions) {
                    d.addClass("alert-close");
                    d.on("transitionend webkitTransitionEnd oTransitionEnd", function(a) {
                        c(this).trigger("close").trigger("close.fndtn.alert").remove();
                        e.callback();
                    });
                } else {
                    d.fadeOut(300, function() {
                        c(this).trigger("close").trigger("close.fndtn.alert").remove();
                        e.callback();
                    });
                }
            });
        },
        reflow: function() {}
    };
})(jQuery, window, window.document);

(function(a, b, c, d) {
    "use strict";
    Foundation.libs.clearing = {
        name: "clearing",
        version: "5.4.5",
        settings: {
            templates: {
                viewing: '<a href="#" class="clearing-close">&times;</a>' + '<div class="visible-img" style="display: none"><div class="clearing-touch-label"></div><img src="data:image/gif;base64,R0lGODlhAQABAAD/ACwAAAAAAQABAAACADs%3D" alt="" />' + '<p class="clearing-caption"></p><a href="#" class="clearing-main-prev"><span></span></a>' + '<a href="#" class="clearing-main-next"><span></span></a></div>'
            },
            // comma delimited list of selectors that, on click, will close clearing,
            // add 'div.clearing-blackout, div.visible-img' to close on background click
            close_selectors: ".clearing-close, div.clearing-blackout",
            // Default to the entire li element.
            open_selectors: "",
            // Image will be skipped in carousel.
            skip_selector: "",
            touch_label: "",
            // event initializers and locks
            init: false,
            locked: false
        },
        init: function(a, b, c) {
            var d = this;
            Foundation.inherit(this, "throttle image_loaded");
            this.bindings(b, c);
            if (d.S(this.scope).is("[" + this.attr_name() + "]")) {
                this.assemble(d.S("li", this.scope));
            } else {
                d.S("[" + this.attr_name() + "]", this.scope).each(function() {
                    d.assemble(d.S("li", this));
                });
            }
        },
        events: function(d) {
            var e = this, f = e.S, g = a(".scroll-container");
            if (g.length > 0) {
                this.scope = g;
            }
            f(this.scope).off(".clearing").on("click.fndtn.clearing", "ul[" + this.attr_name() + "] li " + this.settings.open_selectors, function(a, b, c) {
                var b = b || f(this), c = c || b, d = b.next("li"), g = b.closest("[" + e.attr_name() + "]").data(e.attr_name(true) + "-init"), h = f(a.target);
                a.preventDefault();
                if (!g) {
                    e.init();
                    g = b.closest("[" + e.attr_name() + "]").data(e.attr_name(true) + "-init");
                }
                // if clearing is open and the current image is
                // clicked, go to the next image in sequence
                if (c.hasClass("visible") && b[0] === c[0] && d.length > 0 && e.is_open(b)) {
                    c = d;
                    h = f("img", c);
                }
                // set current and target to the clicked li if not otherwise defined.
                e.open(h, b, c);
                e.update_paddles(c);
            }).on("click.fndtn.clearing", ".clearing-main-next", function(a) {
                e.nav(a, "next");
            }).on("click.fndtn.clearing", ".clearing-main-prev", function(a) {
                e.nav(a, "prev");
            }).on("click.fndtn.clearing", this.settings.close_selectors, function(a) {
                Foundation.libs.clearing.close(a, this);
            });
            a(c).on("keydown.fndtn.clearing", function(a) {
                e.keydown(a);
            });
            f(b).off(".clearing").on("resize.fndtn.clearing", function() {
                e.resize();
            });
            this.swipe_events(d);
        },
        swipe_events: function(a) {
            var b = this, c = b.S;
            c(this.scope).on("touchstart.fndtn.clearing", ".visible-img", function(a) {
                if (!a.touches) {
                    a = a.originalEvent;
                }
                var b = {
                    start_page_x: a.touches[0].pageX,
                    start_page_y: a.touches[0].pageY,
                    start_time: new Date().getTime(),
                    delta_x: 0,
                    is_scrolling: d
                };
                c(this).data("swipe-transition", b);
                a.stopPropagation();
            }).on("touchmove.fndtn.clearing", ".visible-img", function(a) {
                if (!a.touches) {
                    a = a.originalEvent;
                }
                // Ignore pinch/zoom events
                if (a.touches.length > 1 || a.scale && a.scale !== 1) return;
                var d = c(this).data("swipe-transition");
                if (typeof d === "undefined") {
                    d = {};
                }
                d.delta_x = a.touches[0].pageX - d.start_page_x;
                if (Foundation.rtl) {
                    d.delta_x = -d.delta_x;
                }
                if (typeof d.is_scrolling === "undefined") {
                    d.is_scrolling = !!(d.is_scrolling || Math.abs(d.delta_x) < Math.abs(a.touches[0].pageY - d.start_page_y));
                }
                if (!d.is_scrolling && !d.active) {
                    a.preventDefault();
                    var e = d.delta_x < 0 ? "next" : "prev";
                    d.active = true;
                    b.nav(a, e);
                }
            }).on("touchend.fndtn.clearing", ".visible-img", function(a) {
                c(this).data("swipe-transition", {});
                a.stopPropagation();
            });
        },
        assemble: function(b) {
            var c = b.parent();
            if (c.parent().hasClass("carousel")) {
                return;
            }
            c.after('<div id="foundationClearingHolder"></div>');
            var d = c.detach(), e = "";
            if (d[0] == null) {
                return;
            } else {
                e = d[0].outerHTML;
            }
            var f = this.S("#foundationClearingHolder"), g = c.data(this.attr_name(true) + "-init"), h = {
                grid: '<div class="carousel">' + e + "</div>",
                viewing: g.templates.viewing
            }, i = '<div class="clearing-assembled"><div>' + h.viewing + h.grid + "</div></div>", j = this.settings.touch_label;
            if (Modernizr.touch) {
                i = a(i).find(".clearing-touch-label").html(j).end();
            }
            f.after(i).remove();
        },
        open: function(b, d, e) {
            var f = this, g = a(c.body), h = e.closest(".clearing-assembled"), i = f.S("div", h).first(), j = f.S(".visible-img", i), k = f.S("img", j).not(b), l = f.S(".clearing-touch-label", i), m = false;
            // Event to disable scrolling on touch devices when Clearing is activated
            a("body").on("touchmove", function(a) {
                a.preventDefault();
            });
            k.error(function() {
                m = true;
            });
            function n() {
                setTimeout(function() {
                    this.image_loaded(k, function() {
                        if (k.outerWidth() === 1 && !m) {
                            n.call(this);
                        } else {
                            o.call(this, k);
                        }
                    }.bind(this));
                }.bind(this), 100);
            }
            function o(b) {
                var c = a(b);
                c.css("visibility", "visible");
                // toggle the gallery
                g.css("overflow", "hidden");
                h.addClass("clearing-blackout");
                i.addClass("clearing-container");
                j.show();
                this.fix_height(e).caption(f.S(".clearing-caption", j), f.S("img", e)).center_and_label(b, l).shift(d, e, function() {
                    e.closest("li").siblings().removeClass("visible");
                    e.closest("li").addClass("visible");
                });
                j.trigger("opened.fndtn.clearing");
            }
            if (!this.locked()) {
                j.trigger("open.fndtn.clearing");
                // set the image to the selected thumbnail
                k.attr("src", this.load(b)).css("visibility", "hidden");
                n.call(this);
            }
        },
        close: function(b, d) {
            b.preventDefault();
            var e = function(a) {
                if (/blackout/.test(a.selector)) {
                    return a;
                } else {
                    return a.closest(".clearing-blackout");
                }
            }(a(d)), f = a(c.body), g, h;
            if (d === b.target && e) {
                f.css("overflow", "");
                g = a("div", e).first();
                h = a(".visible-img", g);
                h.trigger("close.fndtn.clearing");
                this.settings.prev_index = 0;
                a("ul[" + this.attr_name() + "]", e).attr("style", "").closest(".clearing-blackout").removeClass("clearing-blackout");
                g.removeClass("clearing-container");
                h.hide();
                h.trigger("closed.fndtn.clearing");
            }
            // Event to re-enable scrolling on touch devices
            a("body").off("touchmove");
            return false;
        },
        is_open: function(a) {
            return a.parent().prop("style").length > 0;
        },
        keydown: function(b) {
            var c = a(".clearing-blackout ul[" + this.attr_name() + "]"), d = this.rtl ? 37 : 39, e = this.rtl ? 39 : 37, f = 27;
            if (b.which === d) this.go(c, "next");
            if (b.which === e) this.go(c, "prev");
            if (b.which === f) this.S("a.clearing-close").trigger("click").trigger("click.fndtn.clearing");
        },
        nav: function(b, c) {
            var d = a("ul[" + this.attr_name() + "]", ".clearing-blackout");
            b.preventDefault();
            this.go(d, c);
        },
        resize: function() {
            var b = a("img", ".clearing-blackout .visible-img"), c = a(".clearing-touch-label", ".clearing-blackout");
            if (b.length) {
                this.center_and_label(b, c);
                b.trigger("resized.fndtn.clearing");
            }
        },
        // visual adjustments
        fix_height: function(a) {
            var b = a.parent().children(), c = this;
            b.each(function() {
                var a = c.S(this), b = a.find("img");
                if (a.height() > b.outerHeight()) {
                    a.addClass("fix-height");
                }
            }).closest("ul").width(b.length * 100 + "%");
            return this;
        },
        update_paddles: function(a) {
            a = a.closest("li");
            var b = a.closest(".carousel").siblings(".visible-img");
            if (a.next().length > 0) {
                this.S(".clearing-main-next", b).removeClass("disabled");
            } else {
                this.S(".clearing-main-next", b).addClass("disabled");
            }
            if (a.prev().length > 0) {
                this.S(".clearing-main-prev", b).removeClass("disabled");
            } else {
                this.S(".clearing-main-prev", b).addClass("disabled");
            }
        },
        center_and_label: function(a, b) {
            if (!this.rtl) {
                a.css({
                    marginLeft: -(a.outerWidth() / 2),
                    marginTop: -(a.outerHeight() / 2)
                });
                if (b.length > 0) {
                    b.css({
                        marginLeft: -(b.outerWidth() / 2),
                        marginTop: -(a.outerHeight() / 2) - b.outerHeight() - 10
                    });
                }
            } else {
                a.css({
                    marginRight: -(a.outerWidth() / 2),
                    marginTop: -(a.outerHeight() / 2),
                    left: "auto",
                    right: "50%"
                });
                if (b.length > 0) {
                    b.css({
                        marginRight: -(b.outerWidth() / 2),
                        marginTop: -(a.outerHeight() / 2) - b.outerHeight() - 10,
                        left: "auto",
                        right: "50%"
                    });
                }
            }
            return this;
        },
        // image loading and preloading
        load: function(a) {
            var b;
            if (a[0].nodeName === "A") {
                b = a.attr("href");
            } else {
                b = a.parent().attr("href");
            }
            this.preload(a);
            if (b) return b;
            return a.attr("src");
        },
        preload: function(a) {
            this.img(a.closest("li").next()).img(a.closest("li").prev());
        },
        img: function(a) {
            if (a.length) {
                var b = new Image(), c = this.S("a", a);
                if (c.length) {
                    b.src = c.attr("href");
                } else {
                    b.src = this.S("img", a).attr("src");
                }
            }
            return this;
        },
        // image caption
        caption: function(a, b) {
            var c = b.attr("data-caption");
            if (c) {
                a.html(c).show();
            } else {
                a.text("").hide();
            }
            return this;
        },
        // directional methods
        go: function(a, b) {
            var c = this.S(".visible", a), d = c[b]();
            // Check for skip selector.
            if (this.settings.skip_selector && d.find(this.settings.skip_selector).length != 0) {
                d = d[b]();
            }
            if (d.length) {
                this.S("img", d).trigger("click", [ c, d ]).trigger("click.fndtn.clearing", [ c, d ]).trigger("change.fndtn.clearing");
            }
        },
        shift: function(a, b, c) {
            var d = b.parent(), e = this.settings.prev_index || b.index(), f = this.direction(d, a, b), g = this.rtl ? "right" : "left", h = parseInt(d.css("left"), 10), i = b.outerWidth(), j;
            var k = {};
            // we use jQuery animate instead of CSS transitions because we
            // need a callback to unlock the next animation
            // needs support for RTL **
            if (b.index() !== e && !/skip/.test(f)) {
                if (/left/.test(f)) {
                    this.lock();
                    k[g] = h + i;
                    d.animate(k, 300, this.unlock());
                } else if (/right/.test(f)) {
                    this.lock();
                    k[g] = h - i;
                    d.animate(k, 300, this.unlock());
                }
            } else if (/skip/.test(f)) {
                // the target image is not adjacent to the current image, so
                // do we scroll right or not
                j = b.index() - this.settings.up_count;
                this.lock();
                if (j > 0) {
                    k[g] = -(j * i);
                    d.animate(k, 300, this.unlock());
                } else {
                    k[g] = 0;
                    d.animate(k, 300, this.unlock());
                }
            }
            c();
        },
        direction: function(a, b, c) {
            var d = this.S("li", a), e = d.outerWidth() + d.outerWidth() / 4, f = Math.floor(this.S(".clearing-container").outerWidth() / e) - 1, g = d.index(c), h;
            this.settings.up_count = f;
            if (this.adjacent(this.settings.prev_index, g)) {
                if (g > f && g > this.settings.prev_index) {
                    h = "right";
                } else if (g > f - 1 && g <= this.settings.prev_index) {
                    h = "left";
                } else {
                    h = false;
                }
            } else {
                h = "skip";
            }
            this.settings.prev_index = g;
            return h;
        },
        adjacent: function(a, b) {
            for (var c = b + 1; c >= b - 1; c--) {
                if (c === a) return true;
            }
            return false;
        },
        // lock management
        lock: function() {
            this.settings.locked = true;
        },
        unlock: function() {
            this.settings.locked = false;
        },
        locked: function() {
            return this.settings.locked;
        },
        off: function() {
            this.S(this.scope).off(".fndtn.clearing");
            this.S(b).off(".fndtn.clearing");
        },
        reflow: function() {
            this.init();
        }
    };
})(jQuery, window, window.document);

(function(a, b, c, d) {
    "use strict";
    Foundation.libs.dropdown = {
        name: "dropdown",
        version: "5.4.5",
        settings: {
            active_class: "open",
            mega_class: "mega",
            align: "bottom",
            is_hover: false,
            opened: function() {},
            closed: function() {}
        },
        init: function(a, b, c) {
            Foundation.inherit(this, "throttle");
            this.bindings(b, c);
        },
        events: function(c) {
            var d = this, e = d.S;
            e(this.scope).off(".dropdown").on("click.fndtn.dropdown", "[" + this.attr_name() + "]", function(b) {
                var c = e(this).data(d.attr_name(true) + "-init") || d.settings;
                if (!c.is_hover || Modernizr.touch) {
                    b.preventDefault();
                    d.toggle(a(this));
                }
            }).on("mouseenter.fndtn.dropdown", "[" + this.attr_name() + "], [" + this.attr_name() + "-content]", function(a) {
                var b = e(this), c, f;
                clearTimeout(d.timeout);
                if (b.data(d.data_attr())) {
                    c = e("#" + b.data(d.data_attr()));
                    f = b;
                } else {
                    c = b;
                    f = e("[" + d.attr_name() + "='" + c.attr("id") + "']");
                }
                var g = f.data(d.attr_name(true) + "-init") || d.settings;
                if (e(a.target).data(d.data_attr()) && g.is_hover) {
                    d.closeall.call(d);
                }
                if (g.is_hover) d.open.apply(d, [ c, f ]);
            }).on("mouseleave.fndtn.dropdown", "[" + this.attr_name() + "], [" + this.attr_name() + "-content]", function(a) {
                var b = e(this);
                d.timeout = setTimeout(function() {
                    if (b.data(d.data_attr())) {
                        var a = b.data(d.data_attr(true) + "-init") || d.settings;
                        if (a.is_hover) d.close.call(d, e("#" + b.data(d.data_attr())));
                    } else {
                        var c = e("[" + d.attr_name() + '="' + e(this).attr("id") + '"]'), a = c.data(d.attr_name(true) + "-init") || d.settings;
                        if (a.is_hover) d.close.call(d, b);
                    }
                }.bind(this), 150);
            }).on("click.fndtn.dropdown", function(b) {
                var c = e(b.target).closest("[" + d.attr_name() + "-content]");
                if (e(b.target).closest("[" + d.attr_name() + "]").length > 0) {
                    return;
                }
                if (!e(b.target).data("revealId") && (c.length > 0 && (e(b.target).is("[" + d.attr_name() + "-content]") || a.contains(c.first()[0], b.target)))) {
                    b.stopPropagation();
                    return;
                }
                d.close.call(d, e("[" + d.attr_name() + "-content]"));
            }).on("opened.fndtn.dropdown", "[" + d.attr_name() + "-content]", function() {
                d.settings.opened.call(this);
            }).on("closed.fndtn.dropdown", "[" + d.attr_name() + "-content]", function() {
                d.settings.closed.call(this);
            });
            e(b).off(".dropdown").on("resize.fndtn.dropdown", d.throttle(function() {
                d.resize.call(d);
            }, 50));
            this.resize();
        },
        close: function(b) {
            var c = this;
            b.each(function() {
                var d = a("[" + c.attr_name() + "=" + b[0].id + "]") || a("aria-controls=" + b[0].id + "]");
                d.attr("aria-expanded", "false");
                if (c.S(this).hasClass(c.settings.active_class)) {
                    c.S(this).css(Foundation.rtl ? "right" : "left", "-99999px").attr("aria-hidden", "true").removeClass(c.settings.active_class).prev("[" + c.attr_name() + "]").removeClass(c.settings.active_class).removeData("target");
                    c.S(this).trigger("closed").trigger("closed.fndtn.dropdown", [ b ]);
                }
            });
        },
        closeall: function() {
            var b = this;
            a.each(b.S("[" + this.attr_name() + "-content]"), function() {
                b.close.call(b, b.S(this));
            });
        },
        open: function(a, b) {
            this.css(a.addClass(this.settings.active_class), b);
            a.prev("[" + this.attr_name() + "]").addClass(this.settings.active_class);
            a.data("target", b.get(0)).trigger("opened").trigger("opened.fndtn.dropdown", [ a, b ]);
            a.attr("aria-hidden", "false");
            b.attr("aria-expanded", "true");
            a.focus();
        },
        data_attr: function() {
            if (this.namespace.length > 0) {
                return this.namespace + "-" + this.name;
            }
            return this.name;
        },
        toggle: function(a) {
            var b = this.S("#" + a.data(this.data_attr()));
            if (b.length === 0) {
                // No dropdown found, not continuing
                return;
            }
            this.close.call(this, this.S("[" + this.attr_name() + "-content]").not(b));
            if (b.hasClass(this.settings.active_class)) {
                this.close.call(this, b);
                if (b.data("target") !== a.get(0)) this.open.call(this, b, a);
            } else {
                this.open.call(this, b, a);
            }
        },
        resize: function() {
            var a = this.S("[" + this.attr_name() + "-content].open"), b = this.S("[" + this.attr_name() + "='" + a.attr("id") + "']");
            if (a.length && b.length) {
                this.css(a, b);
            }
        },
        css: function(a, b) {
            var c = Math.max((b.width() - a.width()) / 2, 8), d = b.data(this.attr_name(true) + "-init") || this.settings;
            this.clear_idx();
            if (this.small()) {
                var e = this.dirs.bottom.call(a, b, d);
                a.attr("style", "").removeClass("drop-left drop-right drop-top").css({
                    position: "absolute",
                    width: "95%",
                    "max-width": "none",
                    top: e.top
                });
                a.css(Foundation.rtl ? "right" : "left", c);
            } else {
                this.style(a, b, d);
            }
            return a;
        },
        style: function(b, c, d) {
            var e = a.extend({
                position: "absolute"
            }, this.dirs[d.align].call(b, c, d));
            b.attr("style", "").css(e);
        },
        // return CSS property object
        // `this` is the dropdown
        dirs: {
            // Calculate target offset
            _base: function(a) {
                var b = this.offsetParent(), c = b.offset(), d = a.offset();
                d.top -= c.top;
                d.left -= c.left;
                return d;
            },
            top: function(a, b) {
                var c = Foundation.libs.dropdown, d = c.dirs._base.call(this, a);
                this.addClass("drop-top");
                if (a.outerWidth() < this.outerWidth() || c.small() || this.hasClass(b.mega_menu)) {
                    c.adjust_pip(this, a, b, d);
                }
                if (Foundation.rtl) {
                    return {
                        left: d.left - this.outerWidth() + a.outerWidth(),
                        top: d.top - this.outerHeight()
                    };
                }
                return {
                    left: d.left,
                    top: d.top - this.outerHeight()
                };
            },
            bottom: function(a, b) {
                var c = Foundation.libs.dropdown, d = c.dirs._base.call(this, a);
                if (a.outerWidth() < this.outerWidth() || c.small() || this.hasClass(b.mega_menu)) {
                    c.adjust_pip(this, a, b, d);
                }
                if (c.rtl) {
                    return {
                        left: d.left - this.outerWidth() + a.outerWidth(),
                        top: d.top + a.outerHeight()
                    };
                }
                return {
                    left: d.left,
                    top: d.top + a.outerHeight()
                };
            },
            left: function(a, b) {
                var c = Foundation.libs.dropdown.dirs._base.call(this, a);
                this.addClass("drop-left");
                return {
                    left: c.left - this.outerWidth(),
                    top: c.top
                };
            },
            right: function(a, b) {
                var c = Foundation.libs.dropdown.dirs._base.call(this, a);
                this.addClass("drop-right");
                return {
                    left: c.left + a.outerWidth(),
                    top: c.top
                };
            }
        },
        // Insert rule to style psuedo elements
        adjust_pip: function(a, b, c, d) {
            var e = Foundation.stylesheet, f = 8;
            if (a.hasClass(c.mega_class)) {
                f = d.left + b.outerWidth() / 2 - 8;
            } else if (this.small()) {
                f += d.left - 8;
            }
            this.rule_idx = e.cssRules.length;
            var g = ".f-dropdown.open:before", h = ".f-dropdown.open:after", i = "left: " + f + "px;", j = "left: " + (f - 1) + "px;";
            if (e.insertRule) {
                e.insertRule([ g, "{", i, "}" ].join(" "), this.rule_idx);
                e.insertRule([ h, "{", j, "}" ].join(" "), this.rule_idx + 1);
            } else {
                e.addRule(g, i, this.rule_idx);
                e.addRule(h, j, this.rule_idx + 1);
            }
        },
        // Remove old dropdown rule index
        clear_idx: function() {
            var a = Foundation.stylesheet;
            if (this.rule_idx) {
                a.deleteRule(this.rule_idx);
                a.deleteRule(this.rule_idx);
                delete this.rule_idx;
            }
        },
        small: function() {
            return matchMedia(Foundation.media_queries.small).matches && !matchMedia(Foundation.media_queries.medium).matches;
        },
        off: function() {
            this.S(this.scope).off(".fndtn.dropdown");
            this.S("html, body").off(".fndtn.dropdown");
            this.S(b).off(".fndtn.dropdown");
            this.S("[data-dropdown-content]").off(".fndtn.dropdown");
        },
        reflow: function() {}
    };
})(jQuery, window, window.document);

(function(a, b, c, d) {
    "use strict";
    Foundation.libs.equalizer = {
        name: "equalizer",
        version: "5.4.5",
        settings: {
            use_tallest: true,
            before_height_change: a.noop,
            after_height_change: a.noop,
            equalize_on_stack: false
        },
        init: function(a, b, c) {
            Foundation.inherit(this, "image_loaded");
            this.bindings(b, c);
            this.reflow();
        },
        events: function() {
            this.S(b).off(".equalizer").on("resize.fndtn.equalizer", function(a) {
                this.reflow();
            }.bind(this));
        },
        equalize: function(b) {
            var c = false, d = b.find("[" + this.attr_name() + "-watch]:visible"), e = b.data(this.attr_name(true) + "-init");
            if (d.length === 0) return;
            var f = d.first().offset().top;
            e.before_height_change();
            b.trigger("before-height-change").trigger("before-height-change.fndth.equalizer");
            d.height("inherit");
            d.each(function() {
                var b = a(this);
                if (b.offset().top !== f) {
                    c = true;
                }
            });
            if (e.equalize_on_stack === false) {
                if (c) return;
            }
            var g = d.map(function() {
                return a(this).outerHeight(false);
            }).get();
            if (e.use_tallest) {
                var h = Math.max.apply(null, g);
                d.css("height", h);
            } else {
                var i = Math.min.apply(null, g);
                d.css("height", i);
            }
            e.after_height_change();
            b.trigger("after-height-change").trigger("after-height-change.fndtn.equalizer");
        },
        reflow: function() {
            var b = this;
            this.S("[" + this.attr_name() + "]", this.scope).each(function() {
                var c = a(this);
                b.image_loaded(b.S("img", this), function() {
                    b.equalize(c);
                });
            });
        }
    };
})(jQuery, window, window.document);

(function(a, b, c, d) {
    "use strict";
    Foundation.libs.interchange = {
        name: "interchange",
        version: "5.4.5",
        cache: {},
        images_loaded: false,
        nodes_loaded: false,
        settings: {
            load_attr: "interchange",
            named_queries: {
                "default": "only screen",
                small: Foundation.media_queries.small,
                medium: Foundation.media_queries.medium,
                large: Foundation.media_queries.large,
                xlarge: Foundation.media_queries.xlarge,
                xxlarge: Foundation.media_queries.xxlarge,
                landscape: "only screen and (orientation: landscape)",
                portrait: "only screen and (orientation: portrait)",
                retina: "only screen and (-webkit-min-device-pixel-ratio: 2)," + "only screen and (min--moz-device-pixel-ratio: 2)," + "only screen and (-o-min-device-pixel-ratio: 2/1)," + "only screen and (min-device-pixel-ratio: 2)," + "only screen and (min-resolution: 192dpi)," + "only screen and (min-resolution: 2dppx)"
            },
            directives: {
                replace: function(b, c, d) {
                    // The trigger argument, if called within the directive, fires
                    // an event named after the directive on the element, passing
                    // any parameters along to the event that you pass to trigger.
                    //
                    // ex. trigger(), trigger([a, b, c]), or trigger(a, b, c)
                    //
                    // This allows you to bind a callback like so:
                    // $('#interchangeContainer').on('replace', function (e, a, b, c) {
                    //   console.log($(this).html(), a, b, c);
                    // });
                    if (/IMG/.test(b[0].nodeName)) {
                        var e = b[0].src;
                        if (new RegExp(c, "i").test(e)) return;
                        b[0].src = c;
                        return d(b[0].src);
                    }
                    var f = b.data(this.data_attr + "-last-path"), g = this;
                    if (f == c) return;
                    if (/\.(gif|jpg|jpeg|tiff|png)([?#].*)?/i.test(c)) {
                        a(b).css("background-image", "url(" + c + ")");
                        b.data("interchange-last-path", c);
                        return d(c);
                    }
                    return a.get(c, function(a) {
                        b.html(a);
                        b.data(g.data_attr + "-last-path", c);
                        d();
                    });
                }
            }
        },
        init: function(b, c, d) {
            Foundation.inherit(this, "throttle random_str");
            this.data_attr = this.set_data_attr();
            a.extend(true, this.settings, c, d);
            this.bindings(c, d);
            this.load("images");
            this.load("nodes");
        },
        get_media_hash: function() {
            var a = "";
            for (var b in this.settings.named_queries) {
                a += matchMedia(this.settings.named_queries[b]).matches.toString();
            }
            return a;
        },
        events: function() {
            var c = this, d;
            a(b).off(".interchange").on("resize.fndtn.interchange", c.throttle(function() {
                var a = c.get_media_hash();
                if (a !== d) {
                    c.resize();
                }
                d = a;
            }, 50));
            return this;
        },
        resize: function() {
            var b = this.cache;
            if (!this.images_loaded || !this.nodes_loaded) {
                setTimeout(a.proxy(this.resize, this), 50);
                return;
            }
            for (var c in b) {
                if (b.hasOwnProperty(c)) {
                    var d = this.results(c, b[c]);
                    if (d) {
                        this.settings.directives[d.scenario[1]].call(this, d.el, d.scenario[0], function() {
                            if (arguments[0] instanceof Array) {
                                var a = arguments[0];
                            } else {
                                var a = Array.prototype.slice.call(arguments, 0);
                            }
                            d.el.trigger(d.scenario[1], a);
                        });
                    }
                }
            }
        },
        results: function(a, b) {
            var c = b.length;
            if (c > 0) {
                var d = this.S("[" + this.add_namespace("data-uuid") + '="' + a + '"]');
                while (c--) {
                    var e, f = b[c][2];
                    if (this.settings.named_queries.hasOwnProperty(f)) {
                        e = matchMedia(this.settings.named_queries[f]);
                    } else {
                        e = matchMedia(f);
                    }
                    if (e.matches) {
                        return {
                            el: d,
                            scenario: b[c]
                        };
                    }
                }
            }
            return false;
        },
        load: function(a, b) {
            if (typeof this["cached_" + a] === "undefined" || b) {
                this["update_" + a]();
            }
            return this["cached_" + a];
        },
        update_images: function() {
            var a = this.S("img[" + this.data_attr + "]"), b = a.length, c = b, d = 0, e = this.data_attr;
            this.cache = {};
            this.cached_images = [];
            this.images_loaded = b === 0;
            while (c--) {
                d++;
                if (a[c]) {
                    var f = a[c].getAttribute(e) || "";
                    if (f.length > 0) {
                        this.cached_images.push(a[c]);
                    }
                }
                if (d === b) {
                    this.images_loaded = true;
                    this.enhance("images");
                }
            }
            return this;
        },
        update_nodes: function() {
            var a = this.S("[" + this.data_attr + "]").not("img"), b = a.length, c = b, d = 0, e = this.data_attr;
            this.cached_nodes = [];
            this.nodes_loaded = b === 0;
            while (c--) {
                d++;
                var f = a[c].getAttribute(e) || "";
                if (f.length > 0) {
                    this.cached_nodes.push(a[c]);
                }
                if (d === b) {
                    this.nodes_loaded = true;
                    this.enhance("nodes");
                }
            }
            return this;
        },
        enhance: function(c) {
            var d = this["cached_" + c].length;
            while (d--) {
                this.object(a(this["cached_" + c][d]));
            }
            return a(b).trigger("resize").trigger("resize.fndtn.interchange");
        },
        convert_directive: function(a) {
            var b = this.trim(a);
            if (b.length > 0) {
                return b;
            }
            return "replace";
        },
        parse_scenario: function(a) {
            // This logic had to be made more complex since some users were using commas in the url path
            // So we cannot simply just split on a comma
            var b = a[0].match(/(.+),\s*(\w+)\s*$/), c = a[1];
            if (b) {
                var d = b[1], e = b[2];
            } else {
                var f = a[0].split(/,\s*$/), d = f[0], e = "";
            }
            return [ this.trim(d), this.convert_directive(e), this.trim(c) ];
        },
        object: function(a) {
            var b = this.parse_data_attr(a), c = [], d = b.length;
            if (d > 0) {
                while (d--) {
                    var e = b[d].split(/\((.*?)(\))$/);
                    if (e.length > 1) {
                        var f = this.parse_scenario(e);
                        c.push(f);
                    }
                }
            }
            return this.store(a, c);
        },
        store: function(a, b) {
            var c = this.random_str(), d = a.data(this.add_namespace("uuid", true));
            if (this.cache[d]) return this.cache[d];
            a.attr(this.add_namespace("data-uuid"), c);
            return this.cache[c] = b;
        },
        trim: function(b) {
            if (typeof b === "string") {
                return a.trim(b);
            }
            return b;
        },
        set_data_attr: function(a) {
            if (a) {
                if (this.namespace.length > 0) {
                    return this.namespace + "-" + this.settings.load_attr;
                }
                return this.settings.load_attr;
            }
            if (this.namespace.length > 0) {
                return "data-" + this.namespace + "-" + this.settings.load_attr;
            }
            return "data-" + this.settings.load_attr;
        },
        parse_data_attr: function(a) {
            var b = a.attr(this.attr_name()).split(/\[(.*?)\]/), c = b.length, d = [];
            while (c--) {
                if (b[c].replace(/[\W\d]+/, "").length > 4) {
                    d.push(b[c]);
                }
            }
            return d;
        },
        reflow: function() {
            this.load("images", true);
            this.load("nodes", true);
        }
    };
})(jQuery, window, window.document);

(function(a, b, c, d) {
    "use strict";
    var e = e || false;
    Foundation.libs.joyride = {
        name: "joyride",
        version: "5.4.5",
        defaults: {
            expose: false,
            // turn on or off the expose feature
            modal: true,
            // Whether to cover page with modal during the tour
            keyboard: true,
            // enable left, right and esc keystrokes
            tip_location: "bottom",
            // 'top' or 'bottom' in relation to parent
            nub_position: "auto",
            // override on a per tooltip bases
            scroll_speed: 1500,
            // Page scrolling speed in milliseconds, 0 = no scroll animation
            scroll_animation: "linear",
            // supports 'swing' and 'linear', extend with jQuery UI.
            timer: 0,
            // 0 = no timer , all other numbers = timer in milliseconds
            start_timer_on_click: true,
            // true or false - true requires clicking the first button start the timer
            start_offset: 0,
            // the index of the tooltip you want to start on (index of the li)
            next_button: true,
            // true or false to control whether a next button is used
            prev_button: true,
            // true or false to control whether a prev button is used
            tip_animation: "fade",
            // 'pop' or 'fade' in each tip
            pause_after: [],
            // array of indexes where to pause the tour after
            exposed: [],
            // array of expose elements
            tip_animation_fade_speed: 300,
            // when tipAnimation = 'fade' this is speed in milliseconds for the transition
            cookie_monster: false,
            // true or false to control whether cookies are used
            cookie_name: "joyride",
            // Name the cookie you'll use
            cookie_domain: false,
            // Will this cookie be attached to a domain, ie. '.notableapp.com'
            cookie_expires: 365,
            // set when you would like the cookie to expire.
            tip_container: "body",
            // Where will the tip be attached
            abort_on_close: true,
            // When true, the close event will not fire any callback
            tip_location_patterns: {
                top: [ "bottom" ],
                bottom: [],
                // bottom should not need to be repositioned
                left: [ "right", "top", "bottom" ],
                right: [ "left", "top", "bottom" ]
            },
            post_ride_callback: function() {},
            // A method to call once the tour closes (canceled or complete)
            post_step_callback: function() {},
            // A method to call after each step
            pre_step_callback: function() {},
            // A method to call before each step
            pre_ride_callback: function() {},
            // A method to call before the tour starts (passed index, tip, and cloned exposed element)
            post_expose_callback: function() {},
            // A method to call after an element has been exposed
            template: {
                // HTML segments for tip layout
                link: '<a href="#close" class="joyride-close-tip">&times;</a>',
                timer: '<div class="joyride-timer-indicator-wrap"><span class="joyride-timer-indicator"></span></div>',
                tip: '<div class="joyride-tip-guide"><span class="joyride-nub"></span></div>',
                wrapper: '<div class="joyride-content-wrapper"></div>',
                button: '<a href="#" class="small button joyride-next-tip"></a>',
                prev_button: '<a href="#" class="small button joyride-prev-tip"></a>',
                modal: '<div class="joyride-modal-bg"></div>',
                expose: '<div class="joyride-expose-wrapper"></div>',
                expose_cover: '<div class="joyride-expose-cover"></div>'
            },
            expose_add_class: ""
        },
        init: function(b, c, d) {
            Foundation.inherit(this, "throttle random_str");
            this.settings = this.settings || a.extend({}, this.defaults, d || c);
            this.bindings(c, d);
        },
        go_next: function() {
            if (this.settings.$li.next().length < 1) {
                this.end();
            } else if (this.settings.timer > 0) {
                clearTimeout(this.settings.automate);
                this.hide();
                this.show();
                this.startTimer();
            } else {
                this.hide();
                this.show();
            }
        },
        go_prev: function() {
            if (this.settings.$li.prev().length < 1) {} else if (this.settings.timer > 0) {
                clearTimeout(this.settings.automate);
                this.hide();
                this.show(null, true);
                this.startTimer();
            } else {
                this.hide();
                this.show(null, true);
            }
        },
        events: function() {
            var c = this;
            a(this.scope).off(".joyride").on("click.fndtn.joyride", ".joyride-next-tip, .joyride-modal-bg", function(a) {
                a.preventDefault();
                this.go_next();
            }.bind(this)).on("click.fndtn.joyride", ".joyride-prev-tip", function(a) {
                a.preventDefault();
                this.go_prev();
            }.bind(this)).on("click.fndtn.joyride", ".joyride-close-tip", function(a) {
                a.preventDefault();
                this.end(this.settings.abort_on_close);
            }.bind(this)).on("keyup.joyride", function(a) {
                if (!this.settings.keyboard) return;
                switch (a.which) {
                  case 39:
                    // right arrow
                    a.preventDefault();
                    this.go_next();
                    break;

                  case 37:
                    // left arrow
                    a.preventDefault();
                    this.go_prev();
                    break;

                  case 27:
                    // escape
                    a.preventDefault();
                    this.end(this.settings.abort_on_close);
                }
            }.bind(this));
            a(b).off(".joyride").on("resize.fndtn.joyride", c.throttle(function() {
                if (a("[" + c.attr_name() + "]").length > 0 && c.settings.$next_tip && c.settings.riding) {
                    if (c.settings.exposed.length > 0) {
                        var b = a(c.settings.exposed);
                        b.each(function() {
                            var b = a(this);
                            c.un_expose(b);
                            c.expose(b);
                        });
                    }
                    if (c.is_phone()) {
                        c.pos_phone();
                    } else {
                        c.pos_default(false);
                    }
                }
            }, 100));
        },
        start: function() {
            var b = this, c = a("[" + this.attr_name() + "]", this.scope), d = [ "timer", "scrollSpeed", "startOffset", "tipAnimationFadeSpeed", "cookieExpires" ], e = d.length;
            if (!c.length > 0) return;
            if (!this.settings.init) this.events();
            this.settings = c.data(this.attr_name(true) + "-init");
            // non configureable settings
            this.settings.$content_el = c;
            this.settings.$body = a(this.settings.tip_container);
            this.settings.body_offset = a(this.settings.tip_container).position();
            this.settings.$tip_content = this.settings.$content_el.find("> li");
            this.settings.paused = false;
            this.settings.attempts = 0;
            this.settings.riding = true;
            // can we create cookies?
            if (typeof a.cookie !== "function") {
                this.settings.cookie_monster = false;
            }
            // generate the tips and insert into dom.
            if (!this.settings.cookie_monster || this.settings.cookie_monster && !a.cookie(this.settings.cookie_name)) {
                this.settings.$tip_content.each(function(c) {
                    var f = a(this);
                    this.settings = a.extend({}, b.defaults, b.data_options(f));
                    // Make sure that settings parsed from data_options are integers where necessary
                    var g = e;
                    while (g--) {
                        b.settings[d[g]] = parseInt(b.settings[d[g]], 10);
                    }
                    b.create({
                        $li: f,
                        index: c
                    });
                });
                // show first tip
                if (!this.settings.start_timer_on_click && this.settings.timer > 0) {
                    this.show("init");
                    this.startTimer();
                } else {
                    this.show("init");
                }
            }
        },
        resume: function() {
            this.set_li();
            this.show();
        },
        tip_template: function(b) {
            var c, d;
            b.tip_class = b.tip_class || "";
            c = a(this.settings.template.tip).addClass(b.tip_class);
            d = a.trim(a(b.li).html()) + this.prev_button_text(b.prev_button_text, b.index) + this.button_text(b.button_text) + this.settings.template.link + this.timer_instance(b.index);
            c.append(a(this.settings.template.wrapper));
            c.first().attr(this.add_namespace("data-index"), b.index);
            a(".joyride-content-wrapper", c).append(d);
            return c[0];
        },
        timer_instance: function(b) {
            var c;
            if (b === 0 && this.settings.start_timer_on_click && this.settings.timer > 0 || this.settings.timer === 0) {
                c = "";
            } else {
                c = a(this.settings.template.timer)[0].outerHTML;
            }
            return c;
        },
        button_text: function(b) {
            if (this.settings.tip_settings.next_button) {
                b = a.trim(b) || "Next";
                b = a(this.settings.template.button).append(b)[0].outerHTML;
            } else {
                b = "";
            }
            return b;
        },
        prev_button_text: function(b, c) {
            if (this.settings.tip_settings.prev_button) {
                b = a.trim(b) || "Previous";
                // Add the disabled class to the button if it's the first element
                if (c == 0) b = a(this.settings.template.prev_button).append(b).addClass("disabled")[0].outerHTML; else b = a(this.settings.template.prev_button).append(b)[0].outerHTML;
            } else {
                b = "";
            }
            return b;
        },
        create: function(b) {
            this.settings.tip_settings = a.extend({}, this.settings, this.data_options(b.$li));
            var c = b.$li.attr(this.add_namespace("data-button")) || b.$li.attr(this.add_namespace("data-text")), d = b.$li.attr(this.add_namespace("data-button-prev")) || b.$li.attr(this.add_namespace("data-prev-text")), e = b.$li.attr("class"), f = a(this.tip_template({
                tip_class: e,
                index: b.index,
                button_text: c,
                prev_button_text: d,
                li: b.$li
            }));
            a(this.settings.tip_container).append(f);
        },
        show: function(b, c) {
            var e = null;
            // are we paused?
            if (this.settings.$li === d || a.inArray(this.settings.$li.index(), this.settings.pause_after) === -1) {
                // don't go to the next li if the tour was paused
                if (this.settings.paused) {
                    this.settings.paused = false;
                } else {
                    this.set_li(b, c);
                }
                this.settings.attempts = 0;
                if (this.settings.$li.length && this.settings.$target.length > 0) {
                    if (b) {
                        //run when we first start
                        this.settings.pre_ride_callback(this.settings.$li.index(), this.settings.$next_tip);
                        if (this.settings.modal) {
                            this.show_modal();
                        }
                    }
                    this.settings.pre_step_callback(this.settings.$li.index(), this.settings.$next_tip);
                    if (this.settings.modal && this.settings.expose) {
                        this.expose();
                    }
                    this.settings.tip_settings = a.extend({}, this.settings, this.data_options(this.settings.$li));
                    this.settings.timer = parseInt(this.settings.timer, 10);
                    this.settings.tip_settings.tip_location_pattern = this.settings.tip_location_patterns[this.settings.tip_settings.tip_location];
                    // scroll if not modal
                    if (!/body/i.test(this.settings.$target.selector)) {
                        this.scroll_to();
                    }
                    if (this.is_phone()) {
                        this.pos_phone(true);
                    } else {
                        this.pos_default(true);
                    }
                    e = this.settings.$next_tip.find(".joyride-timer-indicator");
                    if (/pop/i.test(this.settings.tip_animation)) {
                        e.width(0);
                        if (this.settings.timer > 0) {
                            this.settings.$next_tip.show();
                            setTimeout(function() {
                                e.animate({
                                    width: e.parent().width()
                                }, this.settings.timer, "linear");
                            }.bind(this), this.settings.tip_animation_fade_speed);
                        } else {
                            this.settings.$next_tip.show();
                        }
                    } else if (/fade/i.test(this.settings.tip_animation)) {
                        e.width(0);
                        if (this.settings.timer > 0) {
                            this.settings.$next_tip.fadeIn(this.settings.tip_animation_fade_speed).show();
                            setTimeout(function() {
                                e.animate({
                                    width: e.parent().width()
                                }, this.settings.timer, "linear");
                            }.bind(this), this.settings.tip_animation_fade_speed);
                        } else {
                            this.settings.$next_tip.fadeIn(this.settings.tip_animation_fade_speed);
                        }
                    }
                    this.settings.$current_tip = this.settings.$next_tip;
                } else if (this.settings.$li && this.settings.$target.length < 1) {
                    this.show();
                } else {
                    this.end();
                }
            } else {
                this.settings.paused = true;
            }
        },
        is_phone: function() {
            return matchMedia(Foundation.media_queries.small).matches && !matchMedia(Foundation.media_queries.medium).matches;
        },
        hide: function() {
            if (this.settings.modal && this.settings.expose) {
                this.un_expose();
            }
            if (!this.settings.modal) {
                a(".joyride-modal-bg").hide();
            }
            // Prevent scroll bouncing...wait to remove from layout
            this.settings.$current_tip.css("visibility", "hidden");
            setTimeout(a.proxy(function() {
                this.hide();
                this.css("visibility", "visible");
            }, this.settings.$current_tip), 0);
            this.settings.post_step_callback(this.settings.$li.index(), this.settings.$current_tip);
        },
        set_li: function(a, b) {
            if (a) {
                this.settings.$li = this.settings.$tip_content.eq(this.settings.start_offset);
                this.set_next_tip();
                this.settings.$current_tip = this.settings.$next_tip;
            } else {
                if (b) this.settings.$li = this.settings.$li.prev(); else this.settings.$li = this.settings.$li.next();
                this.set_next_tip();
            }
            this.set_target();
        },
        set_next_tip: function() {
            this.settings.$next_tip = a(".joyride-tip-guide").eq(this.settings.$li.index());
            this.settings.$next_tip.data("closed", "");
        },
        set_target: function() {
            var b = this.settings.$li.attr(this.add_namespace("data-class")), d = this.settings.$li.attr(this.add_namespace("data-id")), e = function() {
                if (d) {
                    return a(c.getElementById(d));
                } else if (b) {
                    return a("." + b).first();
                } else {
                    return a("body");
                }
            };
            this.settings.$target = e();
        },
        scroll_to: function() {
            var c, d;
            c = a(b).height() / 2;
            d = Math.ceil(this.settings.$target.offset().top - c + this.settings.$next_tip.outerHeight());
            if (d != 0) {
                a("html, body").stop().animate({
                    scrollTop: d
                }, this.settings.scroll_speed, "swing");
            }
        },
        paused: function() {
            return a.inArray(this.settings.$li.index() + 1, this.settings.pause_after) === -1;
        },
        restart: function() {
            this.hide();
            this.settings.$li = d;
            this.show("init");
        },
        pos_default: function(a) {
            var b = this.settings.$next_tip.find(".joyride-nub"), c = Math.ceil(b.outerWidth() / 2), d = Math.ceil(b.outerHeight() / 2), e = a || false;
            // tip must not be "display: none" to calculate position
            if (e) {
                this.settings.$next_tip.css("visibility", "hidden");
                this.settings.$next_tip.show();
            }
            if (!/body/i.test(this.settings.$target.selector)) {
                var f = this.settings.tip_settings.tipAdjustmentY ? parseInt(this.settings.tip_settings.tipAdjustmentY) : 0, g = this.settings.tip_settings.tipAdjustmentX ? parseInt(this.settings.tip_settings.tipAdjustmentX) : 0;
                if (this.bottom()) {
                    if (this.rtl) {
                        this.settings.$next_tip.css({
                            top: this.settings.$target.offset().top + d + this.settings.$target.outerHeight() + f,
                            left: this.settings.$target.offset().left + this.settings.$target.outerWidth() - this.settings.$next_tip.outerWidth() + g
                        });
                    } else {
                        this.settings.$next_tip.css({
                            top: this.settings.$target.offset().top + d + this.settings.$target.outerHeight() + f,
                            left: this.settings.$target.offset().left + g
                        });
                    }
                    this.nub_position(b, this.settings.tip_settings.nub_position, "top");
                } else if (this.top()) {
                    if (this.rtl) {
                        this.settings.$next_tip.css({
                            top: this.settings.$target.offset().top - this.settings.$next_tip.outerHeight() - d + f,
                            left: this.settings.$target.offset().left + this.settings.$target.outerWidth() - this.settings.$next_tip.outerWidth()
                        });
                    } else {
                        this.settings.$next_tip.css({
                            top: this.settings.$target.offset().top - this.settings.$next_tip.outerHeight() - d + f,
                            left: this.settings.$target.offset().left + g
                        });
                    }
                    this.nub_position(b, this.settings.tip_settings.nub_position, "bottom");
                } else if (this.right()) {
                    this.settings.$next_tip.css({
                        top: this.settings.$target.offset().top + f,
                        left: this.settings.$target.outerWidth() + this.settings.$target.offset().left + c + g
                    });
                    this.nub_position(b, this.settings.tip_settings.nub_position, "left");
                } else if (this.left()) {
                    this.settings.$next_tip.css({
                        top: this.settings.$target.offset().top + f,
                        left: this.settings.$target.offset().left - this.settings.$next_tip.outerWidth() - c + g
                    });
                    this.nub_position(b, this.settings.tip_settings.nub_position, "right");
                }
                if (!this.visible(this.corners(this.settings.$next_tip)) && this.settings.attempts < this.settings.tip_settings.tip_location_pattern.length) {
                    b.removeClass("bottom").removeClass("top").removeClass("right").removeClass("left");
                    this.settings.tip_settings.tip_location = this.settings.tip_settings.tip_location_pattern[this.settings.attempts];
                    this.settings.attempts++;
                    this.pos_default();
                }
            } else if (this.settings.$li.length) {
                this.pos_modal(b);
            }
            if (e) {
                this.settings.$next_tip.hide();
                this.settings.$next_tip.css("visibility", "visible");
            }
        },
        pos_phone: function(b) {
            var c = this.settings.$next_tip.outerHeight(), d = this.settings.$next_tip.offset(), e = this.settings.$target.outerHeight(), f = a(".joyride-nub", this.settings.$next_tip), g = Math.ceil(f.outerHeight() / 2), h = b || false;
            f.removeClass("bottom").removeClass("top").removeClass("right").removeClass("left");
            if (h) {
                this.settings.$next_tip.css("visibility", "hidden");
                this.settings.$next_tip.show();
            }
            if (!/body/i.test(this.settings.$target.selector)) {
                if (this.top()) {
                    this.settings.$next_tip.offset({
                        top: this.settings.$target.offset().top - c - g
                    });
                    f.addClass("bottom");
                } else {
                    this.settings.$next_tip.offset({
                        top: this.settings.$target.offset().top + e + g
                    });
                    f.addClass("top");
                }
            } else if (this.settings.$li.length) {
                this.pos_modal(f);
            }
            if (h) {
                this.settings.$next_tip.hide();
                this.settings.$next_tip.css("visibility", "visible");
            }
        },
        pos_modal: function(a) {
            this.center();
            a.hide();
            this.show_modal();
        },
        show_modal: function() {
            if (!this.settings.$next_tip.data("closed")) {
                var b = a(".joyride-modal-bg");
                if (b.length < 1) {
                    a("body").append(this.settings.template.modal).show();
                }
                if (/pop/i.test(this.settings.tip_animation)) {
                    b.show();
                } else {
                    b.fadeIn(this.settings.tip_animation_fade_speed);
                }
            }
        },
        expose: function() {
            var c, d, e, f, g, h = "expose-" + this.random_str(6);
            if (arguments.length > 0 && arguments[0] instanceof a) {
                e = arguments[0];
            } else if (this.settings.$target && !/body/i.test(this.settings.$target.selector)) {
                e = this.settings.$target;
            } else {
                return false;
            }
            if (e.length < 1) {
                if (b.console) {
                    console.error("element not valid", e);
                }
                return false;
            }
            c = a(this.settings.template.expose);
            this.settings.$body.append(c);
            c.css({
                top: e.offset().top,
                left: e.offset().left,
                width: e.outerWidth(true),
                height: e.outerHeight(true)
            });
            d = a(this.settings.template.expose_cover);
            f = {
                zIndex: e.css("z-index"),
                position: e.css("position")
            };
            g = e.attr("class") == null ? "" : e.attr("class");
            e.css("z-index", parseInt(c.css("z-index")) + 1);
            if (f.position == "static") {
                e.css("position", "relative");
            }
            e.data("expose-css", f);
            e.data("orig-class", g);
            e.attr("class", g + " " + this.settings.expose_add_class);
            d.css({
                top: e.offset().top,
                left: e.offset().left,
                width: e.outerWidth(true),
                height: e.outerHeight(true)
            });
            if (this.settings.modal) this.show_modal();
            this.settings.$body.append(d);
            c.addClass(h);
            d.addClass(h);
            e.data("expose", h);
            this.settings.post_expose_callback(this.settings.$li.index(), this.settings.$next_tip, e);
            this.add_exposed(e);
        },
        un_expose: function() {
            var c, d, e, f, g, h = false;
            if (arguments.length > 0 && arguments[0] instanceof a) {
                d = arguments[0];
            } else if (this.settings.$target && !/body/i.test(this.settings.$target.selector)) {
                d = this.settings.$target;
            } else {
                return false;
            }
            if (d.length < 1) {
                if (b.console) {
                    console.error("element not valid", d);
                }
                return false;
            }
            c = d.data("expose");
            e = a("." + c);
            if (arguments.length > 1) {
                h = arguments[1];
            }
            if (h === true) {
                a(".joyride-expose-wrapper,.joyride-expose-cover").remove();
            } else {
                e.remove();
            }
            f = d.data("expose-css");
            if (f.zIndex == "auto") {
                d.css("z-index", "");
            } else {
                d.css("z-index", f.zIndex);
            }
            if (f.position != d.css("position")) {
                if (f.position == "static") {
                    // this is default, no need to set it.
                    d.css("position", "");
                } else {
                    d.css("position", f.position);
                }
            }
            g = d.data("orig-class");
            d.attr("class", g);
            d.removeData("orig-classes");
            d.removeData("expose");
            d.removeData("expose-z-index");
            this.remove_exposed(d);
        },
        add_exposed: function(b) {
            this.settings.exposed = this.settings.exposed || [];
            if (b instanceof a || typeof b === "object") {
                this.settings.exposed.push(b[0]);
            } else if (typeof b == "string") {
                this.settings.exposed.push(b);
            }
        },
        remove_exposed: function(b) {
            var c, d;
            if (b instanceof a) {
                c = b[0];
            } else if (typeof b == "string") {
                c = b;
            }
            this.settings.exposed = this.settings.exposed || [];
            d = this.settings.exposed.length;
            while (d--) {
                if (this.settings.exposed[d] == c) {
                    this.settings.exposed.splice(d, 1);
                    return;
                }
            }
        },
        center: function() {
            var c = a(b);
            this.settings.$next_tip.css({
                top: (c.height() - this.settings.$next_tip.outerHeight()) / 2 + c.scrollTop(),
                left: (c.width() - this.settings.$next_tip.outerWidth()) / 2 + c.scrollLeft()
            });
            return true;
        },
        bottom: function() {
            return /bottom/i.test(this.settings.tip_settings.tip_location);
        },
        top: function() {
            return /top/i.test(this.settings.tip_settings.tip_location);
        },
        right: function() {
            return /right/i.test(this.settings.tip_settings.tip_location);
        },
        left: function() {
            return /left/i.test(this.settings.tip_settings.tip_location);
        },
        corners: function(c) {
            var d = a(b), e = d.height() / 2, //using this to calculate since scroll may not have finished yet.
            f = Math.ceil(this.settings.$target.offset().top - e + this.settings.$next_tip.outerHeight()), g = d.width() + d.scrollLeft(), h = d.height() + f, i = d.height() + d.scrollTop(), j = d.scrollTop();
            if (f < j) {
                if (f < 0) {
                    j = 0;
                } else {
                    j = f;
                }
            }
            if (h > i) {
                i = h;
            }
            return [ c.offset().top < j, g < c.offset().left + c.outerWidth(), i < c.offset().top + c.outerHeight(), d.scrollLeft() > c.offset().left ];
        },
        visible: function(a) {
            var b = a.length;
            while (b--) {
                if (a[b]) return false;
            }
            return true;
        },
        nub_position: function(a, b, c) {
            if (b === "auto") {
                a.addClass(c);
            } else {
                a.addClass(b);
            }
        },
        startTimer: function() {
            if (this.settings.$li.length) {
                this.settings.automate = setTimeout(function() {
                    this.hide();
                    this.show();
                    this.startTimer();
                }.bind(this), this.settings.timer);
            } else {
                clearTimeout(this.settings.automate);
            }
        },
        end: function(b) {
            if (this.settings.cookie_monster) {
                a.cookie(this.settings.cookie_name, "ridden", {
                    expires: this.settings.cookie_expires,
                    domain: this.settings.cookie_domain
                });
            }
            if (this.settings.timer > 0) {
                clearTimeout(this.settings.automate);
            }
            if (this.settings.modal && this.settings.expose) {
                this.un_expose();
            }
            // Unplug keystrokes listener
            a(this.scope).off("keyup.joyride");
            this.settings.$next_tip.data("closed", true);
            this.settings.riding = false;
            a(".joyride-modal-bg").hide();
            this.settings.$current_tip.hide();
            if (typeof b === "undefined" || b === false) {
                this.settings.post_step_callback(this.settings.$li.index(), this.settings.$current_tip);
                this.settings.post_ride_callback(this.settings.$li.index(), this.settings.$current_tip);
            }
            a(".joyride-tip-guide").remove();
        },
        off: function() {
            a(this.scope).off(".joyride");
            a(b).off(".joyride");
            a(".joyride-close-tip, .joyride-next-tip, .joyride-modal-bg").off(".joyride");
            a(".joyride-tip-guide, .joyride-modal-bg").remove();
            clearTimeout(this.settings.automate);
            this.settings = {};
        },
        reflow: function() {}
    };
})(jQuery, window, window.document);

(function(a, b, c, d) {
    "use strict";
    Foundation.libs["magellan-expedition"] = {
        name: "magellan-expedition",
        version: "5.4.5",
        settings: {
            active_class: "active",
            threshold: 0,
            // pixels from the top of the expedition for it to become fixes
            destination_threshold: 20,
            // pixels from the top of destination for it to be considered active
            throttle_delay: 30,
            // calculation throttling to increase framerate
            fixed_top: 0
        },
        init: function(a, b, c) {
            Foundation.inherit(this, "throttle");
            this.bindings(b, c);
        },
        events: function() {
            var c = this, d = c.S, e = c.settings;
            // initialize expedition offset
            c.set_expedition_position();
            d(c.scope).off(".magellan").on("click.fndtn.magellan", "[" + c.add_namespace("data-magellan-arrival") + '] a[href^="#"]', function(b) {
                b.preventDefault();
                var d = a(this).closest("[" + c.attr_name() + "]"), e = d.data("magellan-expedition-init"), f = this.hash.split("#").join(""), g = a("a[name='" + f + "']");
                if (g.length === 0) {
                    g = a("#" + f);
                }
                // Account for expedition height if fixed position
                var h = g.offset().top - e.destination_threshold + 1;
                h = h - d.outerHeight();
                a("html, body").stop().animate({
                    scrollTop: h
                }, 700, "swing", function() {
                    if (history.pushState) {
                        history.pushState(null, null, "#" + f);
                    } else {
                        location.hash = "#" + f;
                    }
                });
            }).on("scroll.fndtn.magellan", c.throttle(this.check_for_arrivals.bind(this), e.throttle_delay));
            a(b).on("resize.fndtn.magellan", c.throttle(this.set_expedition_position.bind(this), e.throttle_delay));
        },
        check_for_arrivals: function() {
            var a = this;
            a.update_arrivals();
            a.update_expedition_positions();
        },
        set_expedition_position: function() {
            var b = this;
            a("[" + this.attr_name() + "=fixed]", b.scope).each(function(c, d) {
                var e = a(this), f = e.data("magellan-expedition-init"), g = e.attr("styles"), // save styles
                h, i;
                e.attr("style", "");
                h = e.offset().top + f.threshold;
                //set fixed-top by attribute
                i = parseInt(e.data("magellan-fixed-top"));
                if (!isNaN(i)) b.settings.fixed_top = i;
                e.data(b.data_attr("magellan-top-offset"), h);
                e.attr("style", g);
            });
        },
        update_expedition_positions: function() {
            var c = this, d = a(b).scrollTop();
            a("[" + this.attr_name() + "=fixed]", c.scope).each(function() {
                var b = a(this), e = b.data("magellan-expedition-init"), f = b.attr("style"), // save styles
                g = b.data("magellan-top-offset");
                //scroll to the top distance
                if (d + c.settings.fixed_top >= g) {
                    // Placeholder allows height calculations to be consistent even when
                    // appearing to switch between fixed/non-fixed placement
                    var h = b.prev("[" + c.add_namespace("data-magellan-expedition-clone") + "]");
                    if (h.length === 0) {
                        h = b.clone();
                        h.removeAttr(c.attr_name());
                        h.attr(c.add_namespace("data-magellan-expedition-clone"), "");
                        b.before(h);
                    }
                    b.css({
                        position: "fixed",
                        top: e.fixed_top
                    }).addClass("fixed");
                } else {
                    b.prev("[" + c.add_namespace("data-magellan-expedition-clone") + "]").remove();
                    b.attr("style", f).css("position", "").css("top", "").removeClass("fixed");
                }
            });
        },
        update_arrivals: function() {
            var c = this, d = a(b).scrollTop();
            a("[" + this.attr_name() + "]", c.scope).each(function() {
                var b = a(this), e = b.data(c.attr_name(true) + "-init"), f = c.offsets(b, d), g = b.find("[" + c.add_namespace("data-magellan-arrival") + "]"), h = false;
                f.each(function(a, d) {
                    if (d.viewport_offset >= d.top_offset) {
                        var f = b.find("[" + c.add_namespace("data-magellan-arrival") + "]");
                        f.not(d.arrival).removeClass(e.active_class);
                        d.arrival.addClass(e.active_class);
                        h = true;
                        return true;
                    }
                });
                if (!h) g.removeClass(e.active_class);
            });
        },
        offsets: function(b, c) {
            var d = this, e = b.data(d.attr_name(true) + "-init"), f = c;
            return b.find("[" + d.add_namespace("data-magellan-arrival") + "]").map(function(c, g) {
                var h = a(this).data(d.data_attr("magellan-arrival")), i = a("[" + d.add_namespace("data-magellan-destination") + "=" + h + "]");
                if (i.length > 0) {
                    var j = Math.floor(i.offset().top - e.destination_threshold - b.outerHeight());
                    return {
                        destination: i,
                        arrival: a(this),
                        top_offset: j,
                        viewport_offset: f
                    };
                }
            }).sort(function(a, b) {
                if (a.top_offset < b.top_offset) return -1;
                if (a.top_offset > b.top_offset) return 1;
                return 0;
            });
        },
        data_attr: function(a) {
            if (this.namespace.length > 0) {
                return this.namespace + "-" + a;
            }
            return a;
        },
        off: function() {
            this.S(this.scope).off(".magellan");
            this.S(b).off(".magellan");
        },
        reflow: function() {
            var b = this;
            // remove placeholder expeditions used for height calculation purposes
            a("[" + b.add_namespace("data-magellan-expedition-clone") + "]", b.scope).remove();
        }
    };
})(jQuery, window, window.document);

(function(a, b, c, d) {
    "use strict";
    Foundation.libs.offcanvas = {
        name: "offcanvas",
        version: "5.4.5",
        settings: {
            open_method: "move",
            close_on_click: false
        },
        init: function(a, b, c) {
            this.bindings(b, c);
        },
        events: function() {
            var b = this, c = b.S, d = "", e = "", f = "";
            if (this.settings.open_method === "move") {
                d = "move-";
                e = "right";
                f = "left";
            } else if (this.settings.open_method === "overlap_single") {
                d = "offcanvas-overlap-";
                e = "right";
                f = "left";
            } else if (this.settings.open_method === "overlap") {
                d = "offcanvas-overlap";
            }
            c(this.scope).off(".offcanvas").on("click.fndtn.offcanvas", ".left-off-canvas-toggle", function(f) {
                b.click_toggle_class(f, d + e);
                if (b.settings.open_method !== "overlap") {
                    c(".left-submenu").removeClass(d + e);
                }
                a(".left-off-canvas-toggle").attr("aria-expanded", "true");
            }).on("click.fndtn.offcanvas", ".left-off-canvas-menu a", function(f) {
                var g = b.get_settings(f);
                var h = c(this).parent();
                if (g.close_on_click && !h.hasClass("has-submenu") && !h.hasClass("back")) {
                    b.hide.call(b, d + e, b.get_wrapper(f));
                    h.parent().removeClass(d + e);
                } else if (c(this).parent().hasClass("has-submenu")) {
                    f.preventDefault();
                    c(this).siblings(".left-submenu").toggleClass(d + e);
                } else if (h.hasClass("back")) {
                    f.preventDefault();
                    h.parent().removeClass(d + e);
                }
                a(".left-off-canvas-toggle").attr("aria-expanded", "true");
            }).on("click.fndtn.offcanvas", ".right-off-canvas-toggle", function(e) {
                b.click_toggle_class(e, d + f);
                if (b.settings.open_method !== "overlap") {
                    c(".right-submenu").removeClass(d + f);
                }
                a(".right-off-canvas-toggle").attr("aria-expanded", "true");
            }).on("click.fndtn.offcanvas", ".right-off-canvas-menu a", function(e) {
                var g = b.get_settings(e);
                var h = c(this).parent();
                if (g.close_on_click && !h.hasClass("has-submenu") && !h.hasClass("back")) {
                    b.hide.call(b, d + f, b.get_wrapper(e));
                    h.parent().removeClass(d + f);
                } else if (c(this).parent().hasClass("has-submenu")) {
                    e.preventDefault();
                    c(this).siblings(".right-submenu").toggleClass(d + f);
                } else if (h.hasClass("back")) {
                    e.preventDefault();
                    h.parent().removeClass(d + f);
                }
                a(".right-off-canvas-toggle").attr("aria-expanded", "true");
            }).on("click.fndtn.offcanvas", ".exit-off-canvas", function(g) {
                b.click_remove_class(g, d + f);
                c(".right-submenu").removeClass(d + f);
                if (e) {
                    b.click_remove_class(g, d + e);
                    c(".left-submenu").removeClass(d + f);
                }
                a(".right-off-canvas-toggle").attr("aria-expanded", "true");
            }).on("click.fndtn.offcanvas", ".exit-off-canvas", function(c) {
                b.click_remove_class(c, d + f);
                a(".left-off-canvas-toggle").attr("aria-expanded", "false");
                if (e) {
                    b.click_remove_class(c, d + e);
                    a(".right-off-canvas-toggle").attr("aria-expanded", "false");
                }
            });
        },
        toggle: function(a, b) {
            b = b || this.get_wrapper();
            if (b.is("." + a)) {
                this.hide(a, b);
            } else {
                this.show(a, b);
            }
        },
        show: function(a, b) {
            b = b || this.get_wrapper();
            b.trigger("open").trigger("open.fndtn.offcanvas");
            b.addClass(a);
        },
        hide: function(a, b) {
            b = b || this.get_wrapper();
            b.trigger("close").trigger("close.fndtn.offcanvas");
            b.removeClass(a);
        },
        click_toggle_class: function(a, b) {
            a.preventDefault();
            var c = this.get_wrapper(a);
            this.toggle(b, c);
        },
        click_remove_class: function(a, b) {
            a.preventDefault();
            var c = this.get_wrapper(a);
            this.hide(b, c);
        },
        get_settings: function(a) {
            var b = this.S(a.target).closest("[" + this.attr_name() + "]");
            return b.data(this.attr_name(true) + "-init") || this.settings;
        },
        get_wrapper: function(a) {
            var b = this.S(a ? a.target : this.scope).closest(".off-canvas-wrap");
            if (b.length === 0) {
                b = this.S(".off-canvas-wrap");
            }
            return b;
        },
        reflow: function() {}
    };
})(jQuery, window, window.document);

(function(a, b, c, d) {
    "use strict";
    var e = function() {};
    var f = function(e, f) {
        // Don't reinitialize plugin
        if (e.hasClass(f.slides_container_class)) {
            return this;
        }
        var j = this, k, l = e, m, n, o, p = 0, q, r, s = false, t = false;
        j.slides = function() {
            return l.children(f.slide_selector);
        };
        j.slides().first().addClass(f.active_slide_class);
        j.update_slide_number = function(b) {
            if (f.slide_number) {
                m.find("span:first").text(parseInt(b) + 1);
                m.find("span:last").text(j.slides().length);
            }
            if (f.bullets) {
                n.children().removeClass(f.bullets_active_class);
                a(n.children().get(b)).addClass(f.bullets_active_class);
            }
        };
        j.update_active_link = function(b) {
            var c = a('[data-orbit-link="' + j.slides().eq(b).attr("data-orbit-slide") + '"]');
            c.siblings().removeClass(f.bullets_active_class);
            c.addClass(f.bullets_active_class);
        };
        j.build_markup = function() {
            l.wrap('<div class="' + f.container_class + '"></div>');
            k = l.parent();
            l.addClass(f.slides_container_class);
            if (f.stack_on_small) {
                k.addClass(f.stack_on_small_class);
            }
            if (f.navigation_arrows) {
                k.append(a('<a href="#"><span></span></a>').addClass(f.prev_class));
                k.append(a('<a href="#"><span></span></a>').addClass(f.next_class));
            }
            if (f.timer) {
                o = a("<div>").addClass(f.timer_container_class);
                o.append("<span>");
                o.append(a("<div>").addClass(f.timer_progress_class));
                o.addClass(f.timer_paused_class);
                k.append(o);
            }
            if (f.slide_number) {
                m = a("<div>").addClass(f.slide_number_class);
                m.append("<span></span> " + f.slide_number_text + " <span></span>");
                k.append(m);
            }
            if (f.bullets) {
                n = a("<ol>").addClass(f.bullets_container_class);
                k.append(n);
                n.wrap('<div class="orbit-bullets-container"></div>');
                j.slides().each(function(b, c) {
                    var d = a("<li>").attr("data-orbit-slide", b).on("click", j.link_bullet);
                    n.append(d);
                });
            }
        };
        j._goto = function(b, c) {
            // if (locked) {return false;}
            if (b === p) {
                return false;
            }
            if (typeof r === "object") {
                r.restart();
            }
            var d = j.slides();
            var e = "next";
            s = true;
            if (b < p) {
                e = "prev";
            }
            if (b >= d.length) {
                if (!f.circular) return false;
                b = 0;
            } else if (b < 0) {
                if (!f.circular) return false;
                b = d.length - 1;
            }
            var g = a(d.get(p));
            var h = a(d.get(b));
            g.css("zIndex", 2);
            g.removeClass(f.active_slide_class);
            h.css("zIndex", 4).addClass(f.active_slide_class);
            l.trigger("before-slide-change.fndtn.orbit");
            f.before_slide_change();
            j.update_active_link(b);
            var i = function() {
                var a = function() {
                    p = b;
                    s = false;
                    if (c === true) {
                        r = j.create_timer();
                        r.start();
                    }
                    j.update_slide_number(p);
                    l.trigger("after-slide-change.fndtn.orbit", [ {
                        slide_number: p,
                        total_slides: d.length
                    } ]);
                    f.after_slide_change(p, d.length);
                };
                if (l.height() != h.height() && f.variable_height) {
                    l.animate({
                        height: h.height()
                    }, 250, "linear", a);
                } else {
                    a();
                }
            };
            if (d.length === 1) {
                i();
                return false;
            }
            var k = function() {
                if (e === "next") {
                    q.next(g, h, i);
                }
                if (e === "prev") {
                    q.prev(g, h, i);
                }
            };
            if (h.height() > l.height() && f.variable_height) {
                l.animate({
                    height: h.height()
                }, 250, "linear", k);
            } else {
                k();
            }
        };
        j.next = function(a) {
            a.stopImmediatePropagation();
            a.preventDefault();
            j._goto(p + 1);
        };
        j.prev = function(a) {
            a.stopImmediatePropagation();
            a.preventDefault();
            j._goto(p - 1);
        };
        j.link_custom = function(b) {
            b.preventDefault();
            var c = a(this).attr("data-orbit-link");
            if (typeof c === "string" && (c = a.trim(c)) != "") {
                var d = k.find("[data-orbit-slide=" + c + "]");
                if (d.index() != -1) {
                    j._goto(d.index());
                }
            }
        };
        j.link_bullet = function(b) {
            var c = a(this).attr("data-orbit-slide");
            if (typeof c === "string" && (c = a.trim(c)) != "") {
                if (isNaN(parseInt(c))) {
                    var d = k.find("[data-orbit-slide=" + c + "]");
                    if (d.index() != -1) {
                        j._goto(d.index() + 1);
                    }
                } else {
                    j._goto(parseInt(c));
                }
            }
        };
        j.timer_callback = function() {
            j._goto(p + 1, true);
        };
        j.compute_dimensions = function() {
            var b = a(j.slides().get(p));
            var c = b.height();
            if (!f.variable_height) {
                j.slides().each(function() {
                    if (a(this).height() > c) {
                        c = a(this).height();
                    }
                });
            }
            l.height(c);
        };
        j.create_timer = function() {
            var a = new g(k.find("." + f.timer_container_class), f, j.timer_callback);
            return a;
        };
        j.stop_timer = function() {
            if (typeof r === "object") r.stop();
        };
        j.toggle_timer = function() {
            var a = k.find("." + f.timer_container_class);
            if (a.hasClass(f.timer_paused_class)) {
                if (typeof r === "undefined") {
                    r = j.create_timer();
                }
                r.start();
            } else {
                if (typeof r === "object") {
                    r.stop();
                }
            }
        };
        j.init = function() {
            j.build_markup();
            if (f.timer) {
                r = j.create_timer();
                Foundation.utils.image_loaded(this.slides().children("img"), r.start);
            }
            q = new i(f, l);
            if (f.animation === "slide") q = new h(f, l);
            k.on("click", "." + f.next_class, j.next);
            k.on("click", "." + f.prev_class, j.prev);
            if (f.next_on_click) {
                k.on("click", "." + f.slides_container_class + " [data-orbit-slide]", j.link_bullet);
            }
            k.on("click", j.toggle_timer);
            if (f.swipe) {
                k.on("touchstart.fndtn.orbit", function(a) {
                    if (!a.touches) {
                        a = a.originalEvent;
                    }
                    var b = {
                        start_page_x: a.touches[0].pageX,
                        start_page_y: a.touches[0].pageY,
                        start_time: new Date().getTime(),
                        delta_x: 0,
                        is_scrolling: d
                    };
                    k.data("swipe-transition", b);
                    a.stopPropagation();
                }).on("touchmove.fndtn.orbit", function(a) {
                    if (!a.touches) {
                        a = a.originalEvent;
                    }
                    // Ignore pinch/zoom events
                    if (a.touches.length > 1 || a.scale && a.scale !== 1) return;
                    var b = k.data("swipe-transition");
                    if (typeof b === "undefined") {
                        b = {};
                    }
                    b.delta_x = a.touches[0].pageX - b.start_page_x;
                    if (typeof b.is_scrolling === "undefined") {
                        b.is_scrolling = !!(b.is_scrolling || Math.abs(b.delta_x) < Math.abs(a.touches[0].pageY - b.start_page_y));
                    }
                    if (!b.is_scrolling && !b.active) {
                        a.preventDefault();
                        var c = b.delta_x < 0 ? p + 1 : p - 1;
                        b.active = true;
                        j._goto(c);
                    }
                }).on("touchend.fndtn.orbit", function(a) {
                    k.data("swipe-transition", {});
                    a.stopPropagation();
                });
            }
            k.on("mouseenter.fndtn.orbit", function(a) {
                if (f.timer && f.pause_on_hover) {
                    j.stop_timer();
                }
            }).on("mouseleave.fndtn.orbit", function(a) {
                if (f.timer && f.resume_on_mouseout) {
                    r.start();
                }
            });
            a(c).on("click", "[data-orbit-link]", j.link_custom);
            a(b).on("load resize", j.compute_dimensions);
            Foundation.utils.image_loaded(this.slides().children("img"), j.compute_dimensions);
            Foundation.utils.image_loaded(this.slides().children("img"), function() {
                k.prev("." + f.preloader_class).css("display", "none");
                j.update_slide_number(0);
                j.update_active_link(0);
                l.trigger("ready.fndtn.orbit");
            });
        };
        j.init();
    };
    var g = function(a, b, c) {
        var d = this, e = b.timer_speed, f = a.find("." + b.timer_progress_class), g, h, i = -1;
        this.update_progress = function(a) {
            var b = f.clone();
            b.attr("style", "");
            b.css("width", a + "%");
            f.replaceWith(b);
            f = b;
        };
        this.restart = function() {
            clearTimeout(h);
            a.addClass(b.timer_paused_class);
            i = -1;
            d.update_progress(0);
        };
        this.start = function() {
            if (!a.hasClass(b.timer_paused_class)) {
                return true;
            }
            i = i === -1 ? e : i;
            a.removeClass(b.timer_paused_class);
            g = new Date().getTime();
            f.animate({
                width: "100%"
            }, i, "linear");
            h = setTimeout(function() {
                d.restart();
                c();
            }, i);
            a.trigger("timer-started.fndtn.orbit");
        };
        this.stop = function() {
            if (a.hasClass(b.timer_paused_class)) {
                return true;
            }
            clearTimeout(h);
            a.addClass(b.timer_paused_class);
            var c = new Date().getTime();
            i = i - (c - g);
            var f = 100 - i / e * 100;
            d.update_progress(f);
            a.trigger("timer-stopped.fndtn.orbit");
        };
    };
    var h = function(b, c) {
        var d = b.animation_speed;
        var e = a("html[dir=rtl]").length === 1;
        var f = e ? "marginRight" : "marginLeft";
        var g = {};
        g[f] = "0%";
        this.next = function(a, b, c) {
            a.animate({
                marginLeft: "-100%"
            }, d);
            b.animate(g, d, function() {
                a.css(f, "100%");
                c();
            });
        };
        this.prev = function(a, b, c) {
            a.animate({
                marginLeft: "100%"
            }, d);
            b.css(f, "-100%");
            b.animate(g, d, function() {
                a.css(f, "100%");
                c();
            });
        };
    };
    var i = function(b, c) {
        var d = b.animation_speed;
        var e = a("html[dir=rtl]").length === 1;
        var f = e ? "marginRight" : "marginLeft";
        this.next = function(a, b, c) {
            b.css({
                margin: "0%",
                opacity: "0.01"
            });
            b.animate({
                opacity: "1"
            }, d, "linear", function() {
                a.css("margin", "100%");
                c();
            });
        };
        this.prev = function(a, b, c) {
            b.css({
                margin: "0%",
                opacity: "0.01"
            });
            b.animate({
                opacity: "1"
            }, d, "linear", function() {
                a.css("margin", "100%");
                c();
            });
        };
    };
    Foundation.libs = Foundation.libs || {};
    Foundation.libs.orbit = {
        name: "orbit",
        version: "5.4.5",
        settings: {
            animation: "slide",
            timer_speed: 1e4,
            pause_on_hover: true,
            resume_on_mouseout: false,
            next_on_click: true,
            animation_speed: 500,
            stack_on_small: false,
            navigation_arrows: true,
            slide_number: true,
            slide_number_text: "of",
            container_class: "orbit-container",
            stack_on_small_class: "orbit-stack-on-small",
            next_class: "orbit-next",
            prev_class: "orbit-prev",
            timer_container_class: "orbit-timer",
            timer_paused_class: "paused",
            timer_progress_class: "orbit-progress",
            slides_container_class: "orbit-slides-container",
            preloader_class: "preloader",
            slide_selector: "*",
            bullets_container_class: "orbit-bullets",
            bullets_active_class: "active",
            slide_number_class: "orbit-slide-number",
            caption_class: "orbit-caption",
            active_slide_class: "active",
            orbit_transition_class: "orbit-transitioning",
            bullets: true,
            circular: true,
            timer: true,
            variable_height: false,
            swipe: true,
            before_slide_change: e,
            after_slide_change: e
        },
        init: function(a, b, c) {
            var d = this;
            this.bindings(b, c);
        },
        events: function(a) {
            var b = new f(this.S(a), this.S(a).data("orbit-init"));
            this.S(a).data(this.name + "-instance", b);
        },
        reflow: function() {
            var a = this;
            if (a.S(a.scope).is("[data-orbit]")) {
                var b = a.S(a.scope);
                var c = b.data(a.name + "-instance");
                c.compute_dimensions();
            } else {
                a.S("[data-orbit]", a.scope).each(function(b, c) {
                    var d = a.S(c);
                    var e = a.data_options(d);
                    var f = d.data(a.name + "-instance");
                    f.compute_dimensions();
                });
            }
        }
    };
})(jQuery, window, window.document);

(function(a, b, c, d) {
    "use strict";
    Foundation.libs.reveal = {
        name: "reveal",
        version: "5.4.5",
        locked: false,
        settings: {
            animation: "fadeAndPop",
            animation_speed: 250,
            close_on_background_click: true,
            close_on_esc: true,
            dismiss_modal_class: "close-reveal-modal",
            bg_class: "reveal-modal-bg",
            root_element: "body",
            open: function() {},
            opened: function() {},
            close: function() {},
            closed: function() {},
            bg: a(".reveal-modal-bg"),
            css: {
                open: {
                    opacity: 0,
                    visibility: "visible",
                    display: "block"
                },
                close: {
                    opacity: 1,
                    visibility: "hidden",
                    display: "none"
                }
            }
        },
        init: function(b, c, d) {
            a.extend(true, this.settings, c, d);
            this.bindings(c, d);
        },
        events: function(a) {
            var b = this, d = b.S;
            d(this.scope).off(".reveal").on("click.fndtn.reveal", "[" + this.add_namespace("data-reveal-id") + "]:not([disabled])", function(a) {
                a.preventDefault();
                if (!b.locked) {
                    var c = d(this), e = c.data(b.data_attr("reveal-ajax"));
                    b.locked = true;
                    if (typeof e === "undefined") {
                        b.open.call(b, c);
                    } else {
                        var f = e === true ? c.attr("href") : e;
                        b.open.call(b, c, {
                            url: f
                        });
                    }
                }
            });
            d(c).on("click.fndtn.reveal", this.close_targets(), function(a) {
                a.preventDefault();
                if (!b.locked) {
                    var c = d("[" + b.attr_name() + "].open").data(b.attr_name(true) + "-init"), e = d(a.target)[0] === d("." + c.bg_class)[0];
                    if (e) {
                        if (c.close_on_background_click) {
                            a.stopPropagation();
                        } else {
                            return;
                        }
                    }
                    b.locked = true;
                    b.close.call(b, e ? d("[" + b.attr_name() + "].open") : d(this).closest("[" + b.attr_name() + "]"));
                }
            });
            if (d("[" + b.attr_name() + "]", this.scope).length > 0) {
                d(this.scope).on("open.fndtn.reveal", this.settings.open).on("opened.fndtn.reveal", this.settings.opened).on("opened.fndtn.reveal", this.open_video).on("close.fndtn.reveal", this.settings.close).on("closed.fndtn.reveal", this.settings.closed).on("closed.fndtn.reveal", this.close_video);
            } else {
                d(this.scope).on("open.fndtn.reveal", "[" + b.attr_name() + "]", this.settings.open).on("opened.fndtn.reveal", "[" + b.attr_name() + "]", this.settings.opened).on("opened.fndtn.reveal", "[" + b.attr_name() + "]", this.open_video).on("close.fndtn.reveal", "[" + b.attr_name() + "]", this.settings.close).on("closed.fndtn.reveal", "[" + b.attr_name() + "]", this.settings.closed).on("closed.fndtn.reveal", "[" + b.attr_name() + "]", this.close_video);
            }
            return true;
        },
        // PATCH #3: turning on key up capture only when a reveal window is open
        key_up_on: function(a) {
            var b = this;
            // PATCH #1: fixing multiple keyup event trigger from single key press
            b.S("body").off("keyup.fndtn.reveal").on("keyup.fndtn.reveal", function(a) {
                var c = b.S("[" + b.attr_name() + "].open"), d = c.data(b.attr_name(true) + "-init") || b.settings;
                // PATCH #2: making sure that the close event can be called only while unlocked,
                //           so that multiple keyup.fndtn.reveal events don't prevent clean closing of the reveal window.
                if (d && a.which === 27 && d.close_on_esc && !b.locked) {
                    // 27 is the keycode for the Escape key
                    b.close.call(b, c);
                }
            });
            return true;
        },
        // PATCH #3: turning on key up capture only when a reveal window is open
        key_up_off: function(a) {
            this.S("body").off("keyup.fndtn.reveal");
            return true;
        },
        open: function(c, d) {
            var e = this, f;
            if (c) {
                if (typeof c.selector !== "undefined") {
                    // Find the named node; only use the first one found, since the rest of the code assumes there's only one node
                    f = e.S("#" + c.data(e.data_attr("reveal-id"))).first();
                } else {
                    f = e.S(this.scope);
                    d = c;
                }
            } else {
                f = e.S(this.scope);
            }
            var g = f.data(e.attr_name(true) + "-init");
            g = g || this.settings;
            if (f.hasClass("open") && c.attr("data-reveal-id") == f.attr("id")) {
                return e.close(f);
            }
            if (!f.hasClass("open")) {
                var h = e.S("[" + e.attr_name() + "].open");
                if (typeof f.data("css-top") === "undefined") {
                    f.data("css-top", parseInt(f.css("top"), 10)).data("offset", this.cache_offset(f));
                }
                this.key_up_on(f);
                // PATCH #3: turning on key up capture only when a reveal window is open
                f.trigger("open").trigger("open.fndtn.reveal");
                if (h.length < 1) {
                    this.toggle_bg(f, true);
                }
                if (typeof d === "string") {
                    d = {
                        url: d
                    };
                }
                if (typeof d === "undefined" || !d.url) {
                    if (h.length > 0) {
                        this.hide(h, g.css.close);
                    }
                    this.show(f, g.css.open);
                } else {
                    var i = typeof d.success !== "undefined" ? d.success : null;
                    a.extend(d, {
                        success: function(b, c, d) {
                            if (a.isFunction(i)) {
                                i(b, c, d);
                            }
                            f.html(b);
                            e.S(f).foundation("section", "reflow");
                            e.S(f).children().foundation();
                            if (h.length > 0) {
                                e.hide(h, g.css.close);
                            }
                            e.show(f, g.css.open);
                        }
                    });
                    a.ajax(d);
                }
            }
            e.S(b).trigger("resize");
        },
        close: function(a) {
            var a = a && a.length ? a : this.S(this.scope), b = this.S("[" + this.attr_name() + "].open"), c = a.data(this.attr_name(true) + "-init") || this.settings;
            if (b.length > 0) {
                this.locked = true;
                this.key_up_off(a);
                // PATCH #3: turning on key up capture only when a reveal window is open
                a.trigger("close").trigger("close.fndtn.reveal");
                this.toggle_bg(a, false);
                this.hide(b, c.css.close, c);
            }
        },
        close_targets: function() {
            var a = "." + this.settings.dismiss_modal_class;
            if (this.settings.close_on_background_click) {
                return a + ", ." + this.settings.bg_class;
            }
            return a;
        },
        toggle_bg: function(b, c) {
            if (this.S("." + this.settings.bg_class).length === 0) {
                this.settings.bg = a("<div />", {
                    "class": this.settings.bg_class
                }).appendTo("body").hide();
            }
            var e = this.settings.bg.filter(":visible").length > 0;
            if (c != e) {
                if (c == d ? e : !c) {
                    this.hide(this.settings.bg);
                } else {
                    this.show(this.settings.bg);
                }
            }
        },
        show: function(c, d) {
            // is modal
            if (d) {
                var f = c.data(this.attr_name(true) + "-init") || this.settings, g = f.root_element;
                if (c.parent(g).length === 0) {
                    var h = c.wrap('<div style="display: none;" />').parent();
                    c.on("closed.fndtn.reveal.wrapped", function() {
                        c.detach().appendTo(h);
                        c.unwrap().unbind("closed.fndtn.reveal.wrapped");
                    });
                    c.detach().appendTo(g);
                }
                var i = e(f.animation);
                if (!i.animate) {
                    this.locked = false;
                }
                if (i.pop) {
                    d.top = a(b).scrollTop() - c.data("offset") + "px";
                    var j = {
                        top: a(b).scrollTop() + c.data("css-top") + "px",
                        opacity: 1
                    };
                    return setTimeout(function() {
                        return c.css(d).animate(j, f.animation_speed, "linear", function() {
                            this.locked = false;
                            c.trigger("opened").trigger("opened.fndtn.reveal");
                        }.bind(this)).addClass("open");
                    }.bind(this), f.animation_speed / 2);
                }
                if (i.fade) {
                    d.top = a(b).scrollTop() + c.data("css-top") + "px";
                    var j = {
                        opacity: 1
                    };
                    return setTimeout(function() {
                        return c.css(d).animate(j, f.animation_speed, "linear", function() {
                            this.locked = false;
                            c.trigger("opened").trigger("opened.fndtn.reveal");
                        }.bind(this)).addClass("open");
                    }.bind(this), f.animation_speed / 2);
                }
                return c.css(d).show().css({
                    opacity: 1
                }).addClass("open").trigger("opened").trigger("opened.fndtn.reveal");
            }
            var f = this.settings;
            // should we animate the background?
            if (e(f.animation).fade) {
                return c.fadeIn(f.animation_speed / 2);
            }
            this.locked = false;
            return c.show();
        },
        hide: function(c, d) {
            // is modal
            if (d) {
                var f = c.data(this.attr_name(true) + "-init");
                f = f || this.settings;
                var g = e(f.animation);
                if (!g.animate) {
                    this.locked = false;
                }
                if (g.pop) {
                    var h = {
                        top: -a(b).scrollTop() - c.data("offset") + "px",
                        opacity: 0
                    };
                    return setTimeout(function() {
                        return c.animate(h, f.animation_speed, "linear", function() {
                            this.locked = false;
                            c.css(d).trigger("closed").trigger("closed.fndtn.reveal");
                        }.bind(this)).removeClass("open");
                    }.bind(this), f.animation_speed / 2);
                }
                if (g.fade) {
                    var h = {
                        opacity: 0
                    };
                    return setTimeout(function() {
                        return c.animate(h, f.animation_speed, "linear", function() {
                            this.locked = false;
                            c.css(d).trigger("closed").trigger("closed.fndtn.reveal");
                        }.bind(this)).removeClass("open");
                    }.bind(this), f.animation_speed / 2);
                }
                return c.hide().css(d).removeClass("open").trigger("closed").trigger("closed.fndtn.reveal");
            }
            var f = this.settings;
            // should we animate the background?
            if (e(f.animation).fade) {
                return c.fadeOut(f.animation_speed / 2);
            }
            return c.hide();
        },
        close_video: function(b) {
            var c = a(".flex-video", b.target), d = a("iframe", c);
            if (d.length > 0) {
                d.attr("data-src", d[0].src);
                d.attr("src", d.attr("src"));
                c.hide();
            }
        },
        open_video: function(b) {
            var c = a(".flex-video", b.target), e = c.find("iframe");
            if (e.length > 0) {
                var f = e.attr("data-src");
                if (typeof f === "string") {
                    e[0].src = e.attr("data-src");
                } else {
                    var g = e[0].src;
                    e[0].src = d;
                    e[0].src = g;
                }
                c.show();
            }
        },
        data_attr: function(a) {
            if (this.namespace.length > 0) {
                return this.namespace + "-" + a;
            }
            return a;
        },
        cache_offset: function(a) {
            var b = a.show().height() + parseInt(a.css("top"), 10);
            a.hide();
            return b;
        },
        off: function() {
            a(this.scope).off(".fndtn.reveal");
        },
        reflow: function() {}
    };
    /*
   * getAnimationData('popAndFade') // {animate: true,  pop: true,  fade: true}
   * getAnimationData('fade')       // {animate: true,  pop: false, fade: true}
   * getAnimationData('pop')        // {animate: true,  pop: true,  fade: false}
   * getAnimationData('foo')        // {animate: false, pop: false, fade: false}
   * getAnimationData(null)         // {animate: false, pop: false, fade: false}
   */
    function e(a) {
        var b = /fade/i.test(a);
        var c = /pop/i.test(a);
        return {
            animate: b || c,
            pop: c,
            fade: b
        };
    }
})(jQuery, window, window.document);

(function(a, b, c, d) {
    "use strict";
    Foundation.libs.slider = {
        name: "slider",
        version: "5.4.5",
        settings: {
            start: 0,
            end: 100,
            step: 1,
            initial: null,
            display_selector: "",
            vertical: false,
            on_change: function() {}
        },
        cache: {},
        init: function(a, b, c) {
            Foundation.inherit(this, "throttle");
            this.bindings(b, c);
            this.reflow();
        },
        events: function() {
            var c = this;
            a(this.scope).off(".slider").on("mousedown.fndtn.slider touchstart.fndtn.slider pointerdown.fndtn.slider", "[" + c.attr_name() + "]:not(.disabled, [disabled]) .range-slider-handle", function(b) {
                if (!c.cache.active) {
                    b.preventDefault();
                    c.set_active_slider(a(b.target));
                }
            }).on("mousemove.fndtn.slider touchmove.fndtn.slider pointermove.fndtn.slider", function(d) {
                if (!!c.cache.active) {
                    d.preventDefault();
                    if (a.data(c.cache.active[0], "settings").vertical) {
                        var e = 0;
                        if (!d.pageY) {
                            e = b.scrollY;
                        }
                        c.calculate_position(c.cache.active, (d.pageY || d.originalEvent.clientY || d.originalEvent.touches[0].clientY || d.currentPoint.y) + e);
                    } else {
                        c.calculate_position(c.cache.active, d.pageX || d.originalEvent.clientX || d.originalEvent.touches[0].clientX || d.currentPoint.x);
                    }
                }
            }).on("mouseup.fndtn.slider touchend.fndtn.slider pointerup.fndtn.slider", function(a) {
                c.remove_active_slider();
            }).on("change.fndtn.slider", function(a) {
                c.settings.on_change();
            });
            c.S(b).on("resize.fndtn.slider", c.throttle(function(a) {
                c.reflow();
            }, 300));
        },
        set_active_slider: function(a) {
            this.cache.active = a;
        },
        remove_active_slider: function() {
            this.cache.active = null;
        },
        calculate_position: function(b, c) {
            var d = this, e = a.data(b[0], "settings"), f = a.data(b[0], "handle_l"), g = a.data(b[0], "handle_o"), h = a.data(b[0], "bar_l"), i = a.data(b[0], "bar_o");
            requestAnimationFrame(function() {
                var a;
                if (Foundation.rtl && !e.vertical) {
                    a = d.limit_to((i + h - c) / h, 0, 1);
                } else {
                    a = d.limit_to((c - i) / h, 0, 1);
                }
                a = e.vertical ? 1 - a : a;
                var f = d.normalized_value(a, e.start, e.end, e.step);
                d.set_ui(b, f);
            });
        },
        set_ui: function(b, c) {
            var d = a.data(b[0], "settings"), e = a.data(b[0], "handle_l"), f = a.data(b[0], "bar_l"), g = this.normalized_percentage(c, d.start, d.end), h = g * (f - e) - 1, i = g * 100;
            if (Foundation.rtl && !d.vertical) {
                h = -h;
            }
            h = d.vertical ? -h + f - e + 1 : h;
            this.set_translate(b, h, d.vertical);
            if (d.vertical) {
                b.siblings(".range-slider-active-segment").css("height", i + "%");
            } else {
                b.siblings(".range-slider-active-segment").css("width", i + "%");
            }
            b.parent().attr(this.attr_name(), c).trigger("change").trigger("change.fndtn.slider");
            b.parent().children("input[type=hidden]").val(c);
            if (!b[0].hasAttribute("aria-valuemin")) {
                b.attr({
                    "aria-valuemin": d.start,
                    "aria-valuemax": d.end
                });
            }
            b.attr("aria-valuenow", c);
        },
        normalized_percentage: function(a, b, c) {
            return Math.min(1, (a - b) / (c - b));
        },
        normalized_value: function(a, b, c, d) {
            var e = c - b, f = a * e, g = (f - f % d) / d, h = f % d, i = h >= d * .5 ? d : 0;
            return g * d + i + b;
        },
        set_translate: function(b, c, d) {
            if (d) {
                a(b).css("-webkit-transform", "translateY(" + c + "px)").css("-moz-transform", "translateY(" + c + "px)").css("-ms-transform", "translateY(" + c + "px)").css("-o-transform", "translateY(" + c + "px)").css("transform", "translateY(" + c + "px)");
            } else {
                a(b).css("-webkit-transform", "translateX(" + c + "px)").css("-moz-transform", "translateX(" + c + "px)").css("-ms-transform", "translateX(" + c + "px)").css("-o-transform", "translateX(" + c + "px)").css("transform", "translateX(" + c + "px)");
            }
        },
        limit_to: function(a, b, c) {
            return Math.min(Math.max(a, b), c);
        },
        initialize_settings: function(b) {
            var c = a.extend({}, this.settings, this.data_options(a(b).parent()));
            if (c.vertical) {
                a.data(b, "bar_o", a(b).parent().offset().top);
                a.data(b, "bar_l", a(b).parent().outerHeight());
                a.data(b, "handle_o", a(b).offset().top);
                a.data(b, "handle_l", a(b).outerHeight());
            } else {
                a.data(b, "bar_o", a(b).parent().offset().left);
                a.data(b, "bar_l", a(b).parent().outerWidth());
                a.data(b, "handle_o", a(b).offset().left);
                a.data(b, "handle_l", a(b).outerWidth());
            }
            a.data(b, "bar", a(b).parent());
            a.data(b, "settings", c);
        },
        set_initial_position: function(b) {
            var c = a.data(b.children(".range-slider-handle")[0], "settings"), d = !!c.initial ? c.initial : Math.floor((c.end - c.start) * .5 / c.step) * c.step + c.start, e = b.children(".range-slider-handle");
            this.set_ui(e, d);
        },
        set_value: function(b) {
            var c = this;
            a("[" + c.attr_name() + "]", this.scope).each(function() {
                a(this).attr(c.attr_name(), b);
            });
            if (!!a(this.scope).attr(c.attr_name())) {
                a(this.scope).attr(c.attr_name(), b);
            }
            c.reflow();
        },
        reflow: function() {
            var b = this;
            b.S("[" + this.attr_name() + "]").each(function() {
                var c = a(this).children(".range-slider-handle")[0], d = a(this).attr(b.attr_name());
                b.initialize_settings(c);
                if (d) {
                    b.set_ui(a(c), parseFloat(d));
                } else {
                    b.set_initial_position(a(this));
                }
            });
        }
    };
})(jQuery, window, window.document);

(function(a, b, c, d) {
    "use strict";
    Foundation.libs.tab = {
        name: "tab",
        version: "5.4.5",
        settings: {
            active_class: "active",
            callback: function() {},
            deep_linking: false,
            scroll_to_content: true,
            is_hover: false
        },
        default_tab_hashes: [],
        init: function(a, b, c) {
            var d = this, e = this.S;
            this.bindings(b, c);
            this.handle_location_hash_change();
            // Store the default active tabs which will be referenced when the
            // location hash is absent, as in the case of navigating the tabs and
            // returning to the first viewing via the browser Back button.
            e("[" + this.attr_name() + "] > .active > a", this.scope).each(function() {
                d.default_tab_hashes.push(this.hash);
            });
        },
        events: function() {
            var a = this, c = this.S;
            var d = function(b) {
                var d = c(this).closest("[" + a.attr_name() + "]").data(a.attr_name(true) + "-init");
                if (!d.is_hover || Modernizr.touch) {
                    b.preventDefault();
                    b.stopPropagation();
                    a.toggle_active_tab(c(this).parent());
                }
            };
            c(this.scope).off(".tab").on("focus.fndtn.tab", "[" + this.attr_name() + "] > * > a", d).on("click.fndtn.tab", "[" + this.attr_name() + "] > * > a", d).on("mouseenter.fndtn.tab", "[" + this.attr_name() + "] > * > a", function(b) {
                var d = c(this).closest("[" + a.attr_name() + "]").data(a.attr_name(true) + "-init");
                if (d.is_hover) a.toggle_active_tab(c(this).parent());
            });
            // Location hash change event
            c(b).on("hashchange.fndtn.tab", function(b) {
                b.preventDefault();
                a.handle_location_hash_change();
            });
        },
        handle_location_hash_change: function() {
            var b = this, c = this.S;
            c("[" + this.attr_name() + "]", this.scope).each(function() {
                var e = c(this).data(b.attr_name(true) + "-init");
                if (e.deep_linking) {
                    // Match the location hash to a label
                    var f;
                    if (e.scroll_to_content) {
                        f = b.scope.location.hash;
                    } else {
                        // prefix the hash to prevent anchor scrolling
                        f = b.scope.location.hash.replace("fndtn-", "");
                    }
                    if (f != "") {
                        // Check whether the location hash references a tab content div or
                        // another element on the page (inside or outside the tab content div)
                        var g = c(f);
                        if (g.hasClass("content") && g.parent().hasClass("tab-content")) {
                            // Tab content div
                            b.toggle_active_tab(a("[" + b.attr_name() + "] > * > a[href=" + f + "]").parent());
                        } else {
                            // Not the tab content div. If inside the tab content, find the
                            // containing tab and toggle it as active.
                            var h = g.closest(".content").attr("id");
                            if (h != d) {
                                b.toggle_active_tab(a("[" + b.attr_name() + "] > * > a[href=#" + h + "]").parent(), f);
                            }
                        }
                    } else {
                        // Reference the default tab hashes which were initialized in the init function
                        for (var i in b.default_tab_hashes) {
                            b.toggle_active_tab(a("[" + b.attr_name() + "] > * > a[href=" + b.default_tab_hashes[i] + "]").parent());
                        }
                    }
                }
            });
        },
        toggle_active_tab: function(e, f) {
            var g = this.S, h = e.closest("[" + this.attr_name() + "]"), i = e.find("a"), j = e.children("a").first(), k = "#" + j.attr("href").split("#")[1], l = g(k), m = e.siblings(), n = h.data(this.attr_name(true) + "-init"), o = function(b) {
                // Light modification of Heydon Pickering's Practical ARIA Examples: http://heydonworks.com/practical_aria_examples/js/a11y.js 
                // define current, previous and next (possible) tabs
                var d = a(this);
                var e = a(this).parents("li").prev().children('[role="tab"]');
                var f = a(this).parents("li").next().children('[role="tab"]');
                var g;
                // find the direction (prev or next)
                switch (b.keyCode) {
                  case 37:
                    g = e;
                    break;

                  case 39:
                    g = f;
                    break;

                  default:
                    g = false;
                    break;
                }
                if (g.length) {
                    d.attr({
                        tabindex: "-1",
                        "aria-selected": null
                    });
                    g.attr({
                        tabindex: "0",
                        "aria-selected": true
                    }).focus();
                }
                // Hide panels
                a('[role="tabpanel"]').attr("aria-hidden", "true");
                // Show panel which corresponds to target
                a("#" + a(c.activeElement).attr("href").substring(1)).attr("aria-hidden", null);
            };
            // allow usage of data-tab-content attribute instead of href
            if (g(this).data(this.data_attr("tab-content"))) {
                k = "#" + g(this).data(this.data_attr("tab-content")).split("#")[1];
                l = g(k);
            }
            if (n.deep_linking) {
                if (n.scroll_to_content) {
                    // retain current hash to scroll to content
                    b.location.hash = f || k;
                    if (f == d || f == k) {
                        e.parent()[0].scrollIntoView();
                    } else {
                        g(k)[0].scrollIntoView();
                    }
                } else {
                    // prefix the hashes so that the browser doesn't scroll down
                    if (f != d) {
                        b.location.hash = "fndtn-" + f.replace("#", "");
                    } else {
                        b.location.hash = "fndtn-" + k.replace("#", "");
                    }
                }
            }
            // WARNING: The activation and deactivation of the tab content must
            // occur after the deep linking in order to properly refresh the browser
            // window (notably in Chrome).
            // Clean up multiple attr instances to done once
            e.addClass(n.active_class).triggerHandler("opened");
            i.attr({
                "aria-selected": "true",
                tabindex: 0
            });
            m.removeClass(n.active_class);
            m.find("a").attr({
                "aria-selected": "false",
                tabindex: -1
            });
            l.siblings().removeClass(n.active_class).attr({
                "aria-hidden": "true",
                tabindex: -1
            }).end().addClass(n.active_class).attr("aria-hidden", "false").find(":first-child").attr("tabindex", 0);
            n.callback(e);
            l.children().attr("tab-index", 0);
            l.triggerHandler("toggled", [ e ]);
            h.triggerHandler("toggled", [ l ]);
            i.on("keydown", o);
        },
        data_attr: function(a) {
            if (this.namespace.length > 0) {
                return this.namespace + "-" + a;
            }
            return a;
        },
        off: function() {},
        reflow: function() {}
    };
})(jQuery, window, window.document);

(function(a, b, c, d) {
    "use strict";
    Foundation.libs.tooltip = {
        name: "tooltip",
        version: "5.4.5",
        settings: {
            additional_inheritable_classes: [],
            tooltip_class: ".tooltip",
            append_to: "body",
            touch_close_text: "Tap To Close",
            disable_for_touch: false,
            hover_delay: 200,
            show_on: "all",
            tip_template: function(a, b) {
                return '<span data-selector="' + a + '" id="' + a + '" class="' + Foundation.libs.tooltip.settings.tooltip_class.substring(1) + '" role="tooltip">' + b + '<span class="nub"></span></span>';
            }
        },
        cache: {},
        init: function(a, b, c) {
            Foundation.inherit(this, "random_str");
            this.bindings(b, c);
        },
        should_show: function(b, c) {
            var d = a.extend({}, this.settings, this.data_options(b));
            if (d.show_on === "all") {
                return true;
            } else if (this.small() && d.show_on === "small") {
                return true;
            } else if (this.medium() && d.show_on === "medium") {
                return true;
            } else if (this.large() && d.show_on === "large") {
                return true;
            }
            return false;
        },
        medium: function() {
            return matchMedia(Foundation.media_queries["medium"]).matches;
        },
        large: function() {
            return matchMedia(Foundation.media_queries["large"]).matches;
        },
        events: function(b) {
            var c = this, d = c.S;
            c.create(this.S(b));
            a(this.scope).off(".tooltip").on("mouseenter.fndtn.tooltip mouseleave.fndtn.tooltip touchstart.fndtn.tooltip MSPointerDown.fndtn.tooltip", "[" + this.attr_name() + "]", function(b) {
                var e = d(this), f = a.extend({}, c.settings, c.data_options(e)), g = false;
                if (Modernizr.touch && /touchstart|MSPointerDown/i.test(b.type) && d(b.target).is("a")) {
                    return false;
                }
                if (/mouse/i.test(b.type) && c.ie_touch(b)) return false;
                if (e.hasClass("open")) {
                    if (Modernizr.touch && /touchstart|MSPointerDown/i.test(b.type)) b.preventDefault();
                    c.hide(e);
                } else {
                    if (f.disable_for_touch && Modernizr.touch && /touchstart|MSPointerDown/i.test(b.type)) {
                        return;
                    } else if (!f.disable_for_touch && Modernizr.touch && /touchstart|MSPointerDown/i.test(b.type)) {
                        b.preventDefault();
                        d(f.tooltip_class + ".open").hide();
                        g = true;
                    }
                    if (/enter|over/i.test(b.type)) {
                        this.timer = setTimeout(function() {
                            var a = c.showTip(e);
                        }.bind(this), c.settings.hover_delay);
                    } else if (b.type === "mouseout" || b.type === "mouseleave") {
                        clearTimeout(this.timer);
                        c.hide(e);
                    } else {
                        c.showTip(e);
                    }
                }
            }).on("mouseleave.fndtn.tooltip touchstart.fndtn.tooltip MSPointerDown.fndtn.tooltip", "[" + this.attr_name() + "].open", function(b) {
                if (/mouse/i.test(b.type) && c.ie_touch(b)) return false;
                if (a(this).data("tooltip-open-event-type") == "touch" && b.type == "mouseleave") {
                    return;
                } else if (a(this).data("tooltip-open-event-type") == "mouse" && /MSPointerDown|touchstart/i.test(b.type)) {
                    c.convert_to_touch(a(this));
                } else {
                    c.hide(a(this));
                }
            }).on("DOMNodeRemoved DOMAttrModified", "[" + this.attr_name() + "]:not(a)", function(a) {
                c.hide(d(this));
            });
        },
        ie_touch: function(a) {
            // How do I distinguish between IE11 and Windows Phone 8?????
            return false;
        },
        showTip: function(a) {
            var b = this.getTip(a);
            if (this.should_show(a, b)) {
                return this.show(a);
            }
            return;
        },
        getTip: function(b) {
            var c = this.selector(b), d = a.extend({}, this.settings, this.data_options(b)), e = null;
            if (c) {
                e = this.S('span[data-selector="' + c + '"]' + d.tooltip_class);
            }
            return typeof e === "object" ? e : false;
        },
        selector: function(a) {
            var b = a.attr("id"), c = a.attr(this.attr_name()) || a.attr("data-selector");
            if ((b && b.length < 1 || !b) && typeof c != "string") {
                c = this.random_str(6);
                a.attr("data-selector", c).attr("aria-describedby", c);
            }
            return b && b.length > 0 ? b : c;
        },
        create: function(c) {
            var d = this, e = a.extend({}, this.settings, this.data_options(c)), f = this.settings.tip_template;
            if (typeof e.tip_template === "string" && b.hasOwnProperty(e.tip_template)) {
                f = b[e.tip_template];
            }
            var g = a(f(this.selector(c), a("<div></div>").html(c.attr("title")).html())), h = this.inheritable_classes(c);
            g.addClass(h).appendTo(e.append_to);
            if (Modernizr.touch) {
                g.append('<span class="tap-to-close">' + e.touch_close_text + "</span>");
                g.on("touchstart.fndtn.tooltip MSPointerDown.fndtn.tooltip", function(a) {
                    d.hide(c);
                });
            }
            c.removeAttr("title").attr("title", "");
        },
        reposition: function(b, c, d) {
            var e, f, g, h, i, j;
            c.css("visibility", "hidden").show();
            e = b.data("width");
            f = c.children(".nub");
            g = f.outerHeight();
            h = f.outerHeight();
            if (this.small()) {
                c.css({
                    width: "100%"
                });
            } else {
                c.css({
                    width: e ? e : "auto"
                });
            }
            j = function(a, b, c, d, e, f) {
                return a.css({
                    top: b ? b : "auto",
                    bottom: d ? d : "auto",
                    left: e ? e : "auto",
                    right: c ? c : "auto"
                }).end();
            };
            j(c, b.offset().top + b.outerHeight() + 10, "auto", "auto", b.offset().left);
            if (this.small()) {
                j(c, b.offset().top + b.outerHeight() + 10, "auto", "auto", 12.5, a(this.scope).width());
                c.addClass("tip-override");
                j(f, -g, "auto", "auto", b.offset().left);
            } else {
                var k = b.offset().left;
                if (Foundation.rtl) {
                    f.addClass("rtl");
                    k = b.offset().left + b.outerWidth() - c.outerWidth();
                }
                j(c, b.offset().top + b.outerHeight() + 10, "auto", "auto", k);
                c.removeClass("tip-override");
                if (d && d.indexOf("tip-top") > -1) {
                    if (Foundation.rtl) f.addClass("rtl");
                    j(c, b.offset().top - c.outerHeight(), "auto", "auto", k).removeClass("tip-override");
                } else if (d && d.indexOf("tip-left") > -1) {
                    j(c, b.offset().top + b.outerHeight() / 2 - c.outerHeight() / 2, "auto", "auto", b.offset().left - c.outerWidth() - g).removeClass("tip-override");
                    f.removeClass("rtl");
                } else if (d && d.indexOf("tip-right") > -1) {
                    j(c, b.offset().top + b.outerHeight() / 2 - c.outerHeight() / 2, "auto", "auto", b.offset().left + b.outerWidth() + g).removeClass("tip-override");
                    f.removeClass("rtl");
                }
            }
            c.css("visibility", "visible").hide();
        },
        small: function() {
            return matchMedia(Foundation.media_queries.small).matches && !matchMedia(Foundation.media_queries.medium).matches;
        },
        inheritable_classes: function(b) {
            var c = a.extend({}, this.settings, this.data_options(b)), d = [ "tip-top", "tip-left", "tip-bottom", "tip-right", "radius", "round" ].concat(c.additional_inheritable_classes), e = b.attr("class"), f = e ? a.map(e.split(" "), function(b, c) {
                if (a.inArray(b, d) !== -1) {
                    return b;
                }
            }).join(" ") : "";
            return a.trim(f);
        },
        convert_to_touch: function(b) {
            var c = this, d = c.getTip(b), e = a.extend({}, c.settings, c.data_options(b));
            if (d.find(".tap-to-close").length === 0) {
                d.append('<span class="tap-to-close">' + e.touch_close_text + "</span>");
                d.on("click.fndtn.tooltip.tapclose touchstart.fndtn.tooltip.tapclose MSPointerDown.fndtn.tooltip.tapclose", function(a) {
                    c.hide(b);
                });
            }
            b.data("tooltip-open-event-type", "touch");
        },
        show: function(a) {
            var b = this.getTip(a);
            if (a.data("tooltip-open-event-type") == "touch") {
                this.convert_to_touch(a);
            }
            this.reposition(a, b, a.attr("class"));
            a.addClass("open");
            b.fadeIn(150);
        },
        hide: function(a) {
            var b = this.getTip(a);
            b.fadeOut(150, function() {
                b.find(".tap-to-close").remove();
                b.off("click.fndtn.tooltip.tapclose MSPointerDown.fndtn.tapclose");
                a.removeClass("open");
            });
        },
        off: function() {
            var b = this;
            this.S(this.scope).off(".fndtn.tooltip");
            this.S(this.settings.tooltip_class).each(function(c) {
                a("[" + b.attr_name() + "]").eq(c).attr("title", a(this).text());
            }).remove();
        },
        reflow: function() {}
    };
})(jQuery, window, window.document);

(function(a, b, c, d) {
    "use strict";
    Foundation.libs.topbar = {
        name: "topbar",
        version: "5.4.5",
        settings: {
            index: 0,
            sticky_class: "sticky",
            custom_back_text: true,
            back_text: "Back",
            mobile_show_parent_link: true,
            is_hover: true,
            scrolltop: true,
            // jump to top when sticky nav menu toggle is clicked
            sticky_on: "all"
        },
        init: function(b, c, d) {
            Foundation.inherit(this, "add_custom_rule register_media throttle");
            var e = this;
            e.register_media("topbar", "foundation-mq-topbar");
            this.bindings(c, d);
            e.S("[" + this.attr_name() + "]", this.scope).each(function() {
                var b = a(this), c = b.data(e.attr_name(true) + "-init"), d = e.S("section, .top-bar-section", this);
                b.data("index", 0);
                var f = b.parent();
                if (f.hasClass("fixed") || e.is_sticky(b, f, c)) {
                    e.settings.sticky_class = c.sticky_class;
                    e.settings.sticky_topbar = b;
                    b.data("height", f.outerHeight());
                    b.data("stickyoffset", f.offset().top);
                } else {
                    b.data("height", b.outerHeight());
                }
                if (!c.assembled) {
                    e.assemble(b);
                }
                if (c.is_hover) {
                    e.S(".has-dropdown", b).addClass("not-click");
                } else {
                    e.S(".has-dropdown", b).removeClass("not-click");
                }
                // Pad body when sticky (scrolled) or fixed.
                e.add_custom_rule(".f-topbar-fixed { padding-top: " + b.data("height") + "px }");
                if (f.hasClass("fixed")) {
                    e.S("body").addClass("f-topbar-fixed");
                }
            });
        },
        is_sticky: function(a, b, c) {
            var d = b.hasClass(c.sticky_class);
            if (d && c.sticky_on === "all") {
                return true;
            } else if (d && this.small() && c.sticky_on === "small") {
                return matchMedia(Foundation.media_queries.small).matches && !matchMedia(Foundation.media_queries.medium).matches && !matchMedia(Foundation.media_queries.large).matches;
            } else if (d && this.medium() && c.sticky_on === "medium") {
                return matchMedia(Foundation.media_queries.small).matches && matchMedia(Foundation.media_queries.medium).matches && !matchMedia(Foundation.media_queries.large).matches;
            } else if (d && this.large() && c.sticky_on === "large") {
                return matchMedia(Foundation.media_queries.small).matches && matchMedia(Foundation.media_queries.medium).matches && matchMedia(Foundation.media_queries.large).matches;
            }
            return false;
        },
        toggle: function(c) {
            var d = this, e;
            if (c) {
                e = d.S(c).closest("[" + this.attr_name() + "]");
            } else {
                e = d.S("[" + this.attr_name() + "]");
            }
            var f = e.data(this.attr_name(true) + "-init");
            var g = d.S("section, .top-bar-section", e);
            if (d.breakpoint()) {
                if (!d.rtl) {
                    g.css({
                        left: "0%"
                    });
                    a(">.name", g).css({
                        left: "100%"
                    });
                } else {
                    g.css({
                        right: "0%"
                    });
                    a(">.name", g).css({
                        right: "100%"
                    });
                }
                d.S("li.moved", g).removeClass("moved");
                e.data("index", 0);
                e.toggleClass("expanded").css("height", "");
            }
            if (f.scrolltop) {
                if (!e.hasClass("expanded")) {
                    if (e.hasClass("fixed")) {
                        e.parent().addClass("fixed");
                        e.removeClass("fixed");
                        d.S("body").addClass("f-topbar-fixed");
                    }
                } else if (e.parent().hasClass("fixed")) {
                    if (f.scrolltop) {
                        e.parent().removeClass("fixed");
                        e.addClass("fixed");
                        d.S("body").removeClass("f-topbar-fixed");
                        b.scrollTo(0, 0);
                    } else {
                        e.parent().removeClass("expanded");
                    }
                }
            } else {
                if (d.is_sticky(e, e.parent(), f)) {
                    e.parent().addClass("fixed");
                }
                if (e.parent().hasClass("fixed")) {
                    if (!e.hasClass("expanded")) {
                        e.removeClass("fixed");
                        e.parent().removeClass("expanded");
                        d.update_sticky_positioning();
                    } else {
                        e.addClass("fixed");
                        e.parent().addClass("expanded");
                        d.S("body").addClass("f-topbar-fixed");
                    }
                }
            }
        },
        timer: null,
        events: function(c) {
            var d = this, e = this.S;
            e(this.scope).off(".topbar").on("click.fndtn.topbar", "[" + this.attr_name() + "] .toggle-topbar", function(a) {
                a.preventDefault();
                d.toggle(this);
            }).on("click.fndtn.topbar", '.top-bar .top-bar-section li a[href^="#"],[' + this.attr_name() + '] .top-bar-section li a[href^="#"]', function(b) {
                var c = a(this).closest("li");
                if (d.breakpoint() && !c.hasClass("back") && !c.hasClass("has-dropdown")) {
                    d.toggle();
                }
            }).on("click.fndtn.topbar", "[" + this.attr_name() + "] li.has-dropdown", function(b) {
                var c = e(this), f = e(b.target), g = c.closest("[" + d.attr_name() + "]"), h = g.data(d.attr_name(true) + "-init");
                if (f.data("revealId")) {
                    d.toggle();
                    return;
                }
                if (d.breakpoint()) return;
                if (h.is_hover && !Modernizr.touch) return;
                b.stopImmediatePropagation();
                if (c.hasClass("hover")) {
                    c.removeClass("hover").find("li").removeClass("hover");
                    c.parents("li.hover").removeClass("hover");
                } else {
                    c.addClass("hover");
                    a(c).siblings().removeClass("hover");
                    if (f[0].nodeName === "A" && f.parent().hasClass("has-dropdown")) {
                        b.preventDefault();
                    }
                }
            }).on("click.fndtn.topbar", "[" + this.attr_name() + "] .has-dropdown>a", function(a) {
                if (d.breakpoint()) {
                    a.preventDefault();
                    var b = e(this), c = b.closest("[" + d.attr_name() + "]"), f = c.find("section, .top-bar-section"), g = b.next(".dropdown").outerHeight(), h = b.closest("li");
                    c.data("index", c.data("index") + 1);
                    h.addClass("moved");
                    if (!d.rtl) {
                        f.css({
                            left: -(100 * c.data("index")) + "%"
                        });
                        f.find(">.name").css({
                            left: 100 * c.data("index") + "%"
                        });
                    } else {
                        f.css({
                            right: -(100 * c.data("index")) + "%"
                        });
                        f.find(">.name").css({
                            right: 100 * c.data("index") + "%"
                        });
                    }
                    c.css("height", b.siblings("ul").outerHeight(true) + c.data("height"));
                }
            });
            e(b).off(".topbar").on("resize.fndtn.topbar", d.throttle(function() {
                d.resize.call(d);
            }, 50)).trigger("resize").trigger("resize.fndtn.topbar").load(function() {
                // Ensure that the offset is calculated after all of the pages resources have loaded
                e(this).trigger("resize.fndtn.topbar");
            });
            e("body").off(".topbar").on("click.fndtn.topbar", function(a) {
                var b = e(a.target).closest("li").closest("li.hover");
                if (b.length > 0) {
                    return;
                }
                e("[" + d.attr_name() + "] li.hover").removeClass("hover");
            });
            // Go up a level on Click
            e(this.scope).on("click.fndtn.topbar", "[" + this.attr_name() + "] .has-dropdown .back", function(a) {
                a.preventDefault();
                var b = e(this), c = b.closest("[" + d.attr_name() + "]"), f = c.find("section, .top-bar-section"), g = c.data(d.attr_name(true) + "-init"), h = b.closest("li.moved"), i = h.parent();
                c.data("index", c.data("index") - 1);
                if (!d.rtl) {
                    f.css({
                        left: -(100 * c.data("index")) + "%"
                    });
                    f.find(">.name").css({
                        left: 100 * c.data("index") + "%"
                    });
                } else {
                    f.css({
                        right: -(100 * c.data("index")) + "%"
                    });
                    f.find(">.name").css({
                        right: 100 * c.data("index") + "%"
                    });
                }
                if (c.data("index") === 0) {
                    c.css("height", "");
                } else {
                    c.css("height", i.outerHeight(true) + c.data("height"));
                }
                setTimeout(function() {
                    h.removeClass("moved");
                }, 300);
            });
            // Show dropdown menus when their items are focused
            e(this.scope).find(".dropdown a").focus(function() {
                a(this).parents(".has-dropdown").addClass("hover");
            }).blur(function() {
                a(this).parents(".has-dropdown").removeClass("hover");
            });
        },
        resize: function() {
            var a = this;
            a.S("[" + this.attr_name() + "]").each(function() {
                var b = a.S(this), d = b.data(a.attr_name(true) + "-init");
                var e = b.parent("." + a.settings.sticky_class);
                var f;
                if (!a.breakpoint()) {
                    var g = b.hasClass("expanded");
                    b.css("height", "").removeClass("expanded").find("li").removeClass("hover");
                    if (g) {
                        a.toggle(b);
                    }
                }
                if (a.is_sticky(b, e, d)) {
                    if (e.hasClass("fixed")) {
                        // Remove the fixed to allow for correct calculation of the offset.
                        e.removeClass("fixed");
                        f = e.offset().top;
                        if (a.S(c.body).hasClass("f-topbar-fixed")) {
                            f -= b.data("height");
                        }
                        b.data("stickyoffset", f);
                        e.addClass("fixed");
                    } else {
                        f = e.offset().top;
                        b.data("stickyoffset", f);
                    }
                }
            });
        },
        breakpoint: function() {
            return !matchMedia(Foundation.media_queries["topbar"]).matches;
        },
        small: function() {
            return matchMedia(Foundation.media_queries["small"]).matches;
        },
        medium: function() {
            return matchMedia(Foundation.media_queries["medium"]).matches;
        },
        large: function() {
            return matchMedia(Foundation.media_queries["large"]).matches;
        },
        assemble: function(b) {
            var c = this, d = b.data(this.attr_name(true) + "-init"), e = c.S("section, .top-bar-section", b);
            // Pull element out of the DOM for manipulation
            e.detach();
            c.S(".has-dropdown>a", e).each(function() {
                var b = c.S(this), e = b.siblings(".dropdown"), f = b.attr("href"), g;
                if (!e.find(".title.back").length) {
                    if (d.mobile_show_parent_link == true && f) {
                        g = a('<li class="title back js-generated"><h5><a href="javascript:void(0)"></a></h5></li><li class="parent-link show-for-small"><a class="parent-link js-generated" href="' + f + '">' + b.html() + "</a></li>");
                    } else {
                        g = a('<li class="title back js-generated"><h5><a href="javascript:void(0)"></a></h5>');
                    }
                    // Copy link to subnav
                    if (d.custom_back_text == true) {
                        a("h5>a", g).html(d.back_text);
                    } else {
                        a("h5>a", g).html("&laquo; " + b.html());
                    }
                    e.prepend(g);
                }
            });
            // Put element back in the DOM
            e.appendTo(b);
            // check for sticky
            this.sticky();
            this.assembled(b);
        },
        assembled: function(b) {
            b.data(this.attr_name(true), a.extend({}, b.data(this.attr_name(true)), {
                assembled: true
            }));
        },
        height: function(b) {
            var c = 0, d = this;
            a("> li", b).each(function() {
                c += d.S(this).outerHeight(true);
            });
            return c;
        },
        sticky: function() {
            var a = this;
            this.S(b).on("scroll", function() {
                a.update_sticky_positioning();
            });
        },
        update_sticky_positioning: function() {
            var a = "." + this.settings.sticky_class, c = this.S(b), d = this;
            if (d.settings.sticky_topbar && d.is_sticky(this.settings.sticky_topbar, this.settings.sticky_topbar.parent(), this.settings)) {
                var e = this.settings.sticky_topbar.data("stickyoffset");
                if (!d.S(a).hasClass("expanded")) {
                    if (c.scrollTop() > e) {
                        if (!d.S(a).hasClass("fixed")) {
                            d.S(a).addClass("fixed");
                            d.S("body").addClass("f-topbar-fixed");
                        }
                    } else if (c.scrollTop() <= e) {
                        if (d.S(a).hasClass("fixed")) {
                            d.S(a).removeClass("fixed");
                            d.S("body").removeClass("f-topbar-fixed");
                        }
                    }
                }
            }
        },
        off: function() {
            this.S(this.scope).off(".fndtn.topbar");
            this.S(b).off(".fndtn.topbar");
        },
        reflow: function() {}
    };
})(jQuery, window, window.document);

jQuery(function(a) {
    "use strict";
    a(document).foundation();
    // Make placeholder appear on IE
    a(function() {
        var b = document.createElement("input");
        if ("placeholder" in b === false) {
            a("[placeholder]").focus(function() {
                var b = a(this);
                if (b.val() == b.attr("placeholder")) {
                    b.val("").removeClass("placeholder");
                    if (b.hasClass("password")) {
                        b.removeClass("password");
                        this.type = "password";
                    }
                }
            }).blur(function() {
                var b = a(this);
                if (b.val() === "" || b.val() == b.attr("placeholder")) {
                    if (this.type == "password") {
                        b.addClass("password");
                        this.type = "text";
                    }
                    b.addClass("placeholder").val(b.attr("placeholder"));
                }
            }).blur().parents("form").submit(function() {
                a(this).find("[placeholder]").each(function() {
                    var b = a(this);
                    if (b.val() == b.attr("placeholder")) {
                        b.val("");
                    }
                });
            });
        }
    });
    // Toggle navigation
    // var $menu = $('#headermenu'),
    // $menulink = $('.nav-toggle');
    // $menulink.click(function() {
    //     $menulink.toggleClass('active');
    //     $menu.toggleClass('active');
    //     return false;
    // });
    // Search
    var b = a(".header-search .icon-search"), c = a(".header-search input");
    c.blur(function() {
        b.removeClass("focus");
    });
    c.focus(function() {
        b.addClass("focus");
    });
    var d = a("#headermenu .button-search"), e = a(".header-search");
    d.click(function() {
        e.slideToggle("fast");
        d.toggleClass("opened");
    });
    // Scroll to commnets
    a("button.comment-scroll").click(function() {
        a("html, body").animate({
            scrollTop: a("#comment-form").offset().top
        }, 1e3);
    });
    // Resonsive nav
    var f = document.body.clientWidth;
    a(document).ready(function() {
        a(".menu li a").each(function() {
            if (a(this).next().length > 0) {
                a(this).addClass("parent");
            }
        });
        a(".nav-toggle").click(function(b) {
            b.preventDefault();
            a(this).toggleClass("active");
            a(".headermenu").toggleClass("active");
            a(".menu").toggle();
        });
        g();
    });
    a(window).bind("resize orientationchange", function() {
        f = document.body.clientWidth;
        g();
    });
    var g = function() {
        if (f < 768) {
            a(".nav-toggle").css("display", "block");
            if (!a(".nav-toggle").hasClass("active")) {
                a(".menu").hide();
            } else {
                a(".menu").show();
            }
            a(".menu li").unbind("mouseenter mouseleave");
            a(".menu li a.parent").unbind("click");
            a(".menu li .more").unbind("click").bind("click", function() {
                a(this).parent("li").toggleClass("hover");
            });
        } else if (f >= 768) {
            a(".nav-toggle").css("display", "none");
            a(".menu").show();
            a(".menu li").removeClass("hover");
            a(".menu li a").unbind("click");
            a(".menu li").unbind("mouseenter mouseleave").bind("mouseenter mouseleave", function() {
                a(this).toggleClass("hover");
            });
        }
    };
});
//# sourceMappingURL=main.min.js.map